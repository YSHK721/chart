{
  "name": "indicator-kit-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Chart front module bodies shared by the live core (indicator_ui) and the replay core (simulator/replay_ui): chart rendering/interaction adapters, indicator & template & theme controllers, position sizing, and the domain/usecase they rest on. Consumers borrow them through symlinks; this package owns the entities. Pure-logic tests run on node:test (node v24).",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'"
  }
}
