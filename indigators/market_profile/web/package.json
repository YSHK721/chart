{
  "name": "market-profile-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Market Profile front module (actor/client/primitive/domain + catalog entry). Pure-logic tests run on node:test (node v24).",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'"
  }
}
