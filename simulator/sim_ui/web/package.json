{
  "name": "sim-ui-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sim UI front layer (display composition root / v5 chart adapter / view / report source). Pure-logic tests run on node:test (node v24).",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'"
  }
}
