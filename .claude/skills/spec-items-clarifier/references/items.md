# Spec Items 詳細リファレンス

各 Item の判断軸・完了基準・アーキタイプ・出力形式の詳細。
SKILL.md §5 step S-1（完了基準突合）と step S-3（オプション生成）実行時に必ず本ファイルを参照する。

## 目次

| Item | 内容 | 判断軸 |
|------|------|--------|
| [Item 1](#item-1-objective--background目的背景) | Objective / Background | 成功をどの種類の基準で測定するか |
| [Item 2](#item-2-scope--out-of-scope範囲対象外) | Scope / Out of Scope | スコープの境界をどの軸で引くか |
| [Item 3](#item-3-assumptions前提条件) | Assumptions | 前提の保証をどのメカニズムで担保するか |
| [Item 4](#item-4-constraints制約事項) | Constraints | 競合する制約のうちどれを不変とするか |
| [Item 5](#item-5-input--trigger入力トリガー) | Input / Trigger | 処理の実行モデル（いつ・誰が・どのように開始するか） |
| [Item 6](#item-6-processing-logic処理内容) | Processing Logic | 処理途中で障害が発生した場合の継続性戦略 |
| [Item 7](#item-7-target-entities対象) | Target Entities | データがどこに存在し、どのアクセスパターンを取るか |
| [Item 8](#item-8-output--result結果出力) | Output / Result | 出力の一時性と到達方法 |
| [Item 9](#item-9-exception-handling例外処理) | Exception Handling | 障害に対するシステムの継続戦略 |

---

## Item 1: Objective / Background（目的・背景）

**判断軸**：成功をどの種類の基準で測定するか

**完了基準**：
- [ ] この機能が存在しない場合に発生する具体的な問題が 1 文で記述されている
- [ ] 達成を判定できる定量的または状態的な成功基準が定義されている
- [ ] ステークホルダー（誰が要求したか）が特定されている

**アーキタイプ**：

| オプション | 名称 | 成功基準のタイプ | 適するケース |
|-----------|------|----------------|------------|
| A | 数値 KPI 型 | 計測可能な数値（時間・件数・率）で判定 | 業務効率化・コスト削減 |
| B | 状態到達型 | 特定の機能・状態が存在することで判定 | 新機能追加・技術移行 |
| C | 問題解消型 | 現在の問題事象が発生しなくなることで判定 | バグ修正・障害対応 |

**出力形式**：

```
1. Objective / Background
   問題: [この機能がなければ発生する具体的な問題（1 文）]
   成功基準: [選択されたオプションに基づく定量的または状態的な判定基準]
   要求元: [ステークホルダー名]
```

---

## Item 2: Scope / Out of Scope（範囲・対象外）

**判断軸**：スコープの境界をどの軸で引くか

**完了基準**：
- [ ] 含む機能・処理が列挙されている
- [ ] 明示的な除外項目が最低 1 件記述されている
- [ ] 境界条件が数値・条件式で定義されている

**アーキタイプ**：

| オプション | 名称 | 境界の引き方 | 除外の明示方法 |
|-----------|------|------------|-------------|
| A | 機能列挙型 | 含む機能を全列挙し、それ以外を全除外と宣言 | 「上記以外は全てスコープ外」 |
| B | ユーザー層型 | 対象ユーザー層を確定し、そのユーザーの行為のみを含む | 除外ユーザー層を明記 |
| C | データ境界型 | 対象データの条件（期間・状態・属性）を確定し、その範囲の処理のみ含む | 除外データ条件を明記 |

**出力形式**：

```
2. Scope / Out of Scope
   含む:
     - [処理・機能 1]
     - [処理・機能 2]
   含まない（明示的除外）:
     - [除外項目 1]
   境界条件: [数値・条件式]
```

---

## Item 3: Assumptions（前提条件）

**判断軸**：前提の保証をどのメカニズムで担保するか

**完了基準**：
- [ ] 外部条件が具体的に記述されている
- [ ] 各前提が「誰によって・いつまでに」保証されるかが明記されている
- [ ] 前提崩壊時の対応方針が定義されている

**アーキタイプ**：

| オプション | 名称 | 保証メカニズム | 崩壊時の対応 |
|-----------|------|-------------|------------|
| A | 契約保証型 | 書面合意・SLA・チケットによる確約 | 合意破棄として設計を凍結し再合意を要求 |
| B | 技術検証型 | ヘルスチェック・自動テスト・CI で継続的に検証 | 検証失敗時にデプロイをブロック |
| C | 期日確認型 | マイルストーン前に担当者が手動確認・承認 | 未確認の場合は当該項目を TBD に戻す |

**出力形式**：

```
3. Assumptions
   前提条件:
     - [前提 1]: 保証元=[チーム/担当者], 保証方法=[選択オプション], 確認期限=[日付]
   前提崩壊時の対応: [選択されたオプションに基づく具体的な手順]
```

---

## Item 4: Constraints（制約事項）

**判断軸**：競合する制約（技術・時間・予算）のうち、どれを不変とするか

**完了基準**：
- [ ] 技術的制約（言語・フレームワーク・インフラ）が列挙されている
- [ ] 時間的制約（リリース日）が日付で定義されている
- [ ] ビジネス・法的制約が明記されているか「なし（確認済み）」と確認済み

**アーキタイプ**：

| オプション | 名称 | 不変の制約 | 交渉可能な制約 |
|-----------|------|----------|-------------|
| A | 技術固定型 | 言語・フレームワーク・インフラを固定 | 日程・スコープを調整可 |
| B | 日程固定型 | リリース日を固定 | 技術選定・機能スコープを削減可 |
| C | 予算固定型 | 工数・コスト上限を固定 | 技術・日程・スコープを調整可 |

**出力形式**：

```
4. Constraints
   技術的制約: [言語・インフラ名]
   時間的制約: リリース期限 = [YYYY-MM-DD]
   ビジネス的制約: [規制名 / 「なし（確認済み）」]
   制約の優先順位: [競合発生時は[〇〇]を優先する]
```

---

## Item 5: Input / Trigger（入力・トリガー）

**判断軸**：処理の実行モデル（いつ・誰が・どのように開始するか）

**完了基準**：
- [ ] トリガー種別が 1 つに確定している
- [ ] 受け取るデータのフィールド名・型・バリデーションが定義されている
- [ ] 送信元が特定されている

**アーキタイプ**：

| オプション | 名称 | トリガー種別 | 実行モデル |
|-----------|------|------------|---------|
| A | 同期即時型 | ユーザー操作または API 呼び出し | リクエスト受信 → 処理 → レスポンス返却（ブロッキング） |
| B | 非同期キュー型 | ユーザー操作または API 呼び出し | リクエスト受信 → ジョブ ID 返却 → バックグラウンド処理 |
| C | スケジュール型 | 時刻・間隔（cron） | 定時起動 → データソースから入力取得 → バッチ処理 |

**出力形式**：

```
5. Input / Trigger
   トリガー: [具体的なエンドポイント / cron 式]
   実行モデル: [選択されたオプションの詳細]
   入力データ:
     | フィールド名 | 型 | 必須/任意 | バリデーション | 例 |
     |------------|-----|---------|-------------|-----|
     | [name]     | [type] | 必須 | [条件]   | [値] |
   送信元: [システム名 / ユーザーロール]
```

---

## Item 6: Processing Logic（処理内容）

**判断軸**：処理途中で障害が発生した場合の継続性戦略

**注意**：本セクションは文脈への依存度が最も高い。ユーザーの説明から処理の概要が読み取れない場合は、以下の事前確認質問を 1 問行ってからオプションを生成する。

> 「処理の具体的な手順を教えてください。入力を受け取った後、どのような順番で何を行いますか？（例: 入力を検証する → DB に保存する → メールを送信する）」

**完了基準**：
- [ ] 処理ステップが番号付きで、主語・動詞・目的語を明示して記述されている
- [ ] すべての分岐条件が「IF [条件式] THEN [処理] ELSE [処理]」の形式で記述されている
- [ ] ループ処理がある場合、終了条件が定義されている
- [ ] 障害時の継続性戦略が明記されている

**アーキタイプ**：

| オプション | 名称 | 継続性戦略 | データの整合性保証 |
|-----------|------|----------|----------------|
| A | アトミック型 | 全ステップが成功した場合のみコミット。1 ステップでも失敗すれば全ロールバック | 強一貫性。部分書き込みが残らない |
| B | ベストエフォート型 | 失敗したステップをスキップして後続を継続。最終的に成功・失敗の件数を報告 | 結果整合性。一部だけ処理された状態になりうる |
| C | チェックポイント型 | ステップ完了ごとに進捗を DB に保存。障害後に再実行すると完了済みステップをスキップ | 冪等性が必要。重複実行を防ぐロジックが前提 |

**出力形式**：

```
6. Processing Logic
   継続性戦略: [選択されたオプション]
   処理ステップ:
     1. [主語]が[対象]を[動詞]する
     2. IF [具体的条件式] THEN [処理] ELSE [処理]
     3. [次のステップ]
   障害発生時: [選択オプションに基づく動作]
```

---

## Item 7: Target Entities（対象）

**判断軸**：データがどこに存在し、どのアクセスパターンを取るか

**完了基準**：
- [ ] 操作対象テーブル・コレクション名が列挙されている
- [ ] 各テーブルの操作種別（READ / INSERT / UPDATE / DELETE）が明記されている
- [ ] 影響を受けるユーザーセグメントが特定されている

**アーキタイプ**：

| オプション | 名称 | データ所在 | アクセスパターン |
|-----------|------|----------|--------------|
| A | ローカル DB 中心型 | 自システムの DB がデータソース | テーブルへの直接 READ/WRITE |
| B | 複数テーブル JOIN 型 | 自システムの複数テーブルを結合 | JOIN クエリ + トランザクション管理 |
| C | 外部 API 中心型 | 外部サービスがデータソース | API 呼び出し + ローカル DB はキャッシュのみ |

**出力形式**：

```
7. Target Entities
   アクセスパターン: [選択されたオプション]
   操作対象:
     | テーブル名 / エンドポイント | 操作種別 | 対象カラム / フィールド |
     |--------------------------|---------|-------------------|
     | [name]                   | READ    | [col1, col2]      |
   影響ユーザー: [条件式で記述。「全ユーザー」は不可]
```

---

## Item 8: Output / Result（結果・出力）

**判断軸**：出力の一時性と到達方法（誰がいつ結果を受け取るか）

**完了基準**：
- [ ] 出力種別が確定している
- [ ] 出力データのフィールド名・型・フォーマットが定義されている
- [ ] 出力の受信者が特定されている

**アーキタイプ**：

| オプション | 名称 | 出力の到達方法 | 結果の永続化 |
|-----------|------|-------------|------------|
| A | 即時レスポンス型 | HTTP レスポンス / 画面表示。処理完了を待って返却 | なし（呼び出し元が保存を担当） |
| B | DB 保存 + ID 返却型 | 結果を DB に保存しレコード ID を返却。内容は参照 API で取得 | あり（自システム DB に永続化） |
| C | 非同期通知型 | ジョブ ID を即時返却。完了後にメール / Webhook で結果を送信 | あり（通知内容 + ステータスを DB に記録） |

**出力形式**：

```
8. Output / Result
   出力モデル: [選択されたオプション]
   出力データ:
     | フィールド名 | 型 | 説明 | 例 |
     |------------|-----|------|-----|
     | [name]     | [type] | [説明] | [値] |
   受信者: [ユーザーロール / システム名]
   永続化: [なし / テーブル名 / 通知チャネル]
```

---

## Item 9: Exception Handling（例外処理）

**判断軸**：障害に対してシステムがどの継続戦略を取るか

**完了基準**：
- [ ] 想定される異常系が最低 3 種類列挙されている
- [ ] 各異常に対するシステム動作が定義されている
- [ ] クライアントへのエラーレスポンス（コード + メッセージ）が定義されている

**アーキタイプ**：

| オプション | 名称 | 障害時の戦略 | データへの影響 |
|-----------|------|-----------|------------|
| A | Fail Fast 型 | 最初のエラーで即座に全処理を停止。ロールバックを実行 | 書き込みは全て元に戻る |
| B | リトライ + 断念型 | 指数バックオフで最大 N 回リトライ。超過後に ERR_MAX_RETRY を返却 | 断念後はロールバック |
| C | 部分成功許容型 | 失敗した個別処理のみスキップ。成功件数 + 失敗件数 + 失敗理由を返却 | 成功分の書き込みは確定 |

**出力形式**：

```
9. Exception Handling
   障害戦略: [選択されたオプション]
   異常系一覧:
     | 異常種別 | 検出条件 | システム動作 | クライアントへの返却 |
     |---------|---------|------------|------------------|
     | [種別]  | [条件]  | [動作]     | [HTTP コード + エラーコード + メッセージ] |
   リトライ設定: [オプション B のみ: 最大 N 回, バックオフ: 2^n 秒, 上限: X 秒]
```
