# upstream-input-validation: sim Phase 9 段階 2 ブランチ準備

実行日時: 2026-08-19
対象タスク: sim Phase 9 段階 2 ブランチ準備

## 上流入力の整理

| 種別 | 件数 | 内容 |
|---|---|---|
| 依頼者指示 | 1 | sim Phase 9 段階 2（sim API の MT5 同期＝strategy ブロック明示拒否）のブランチ準備 |
| 他者レビュー指摘 | 0 | 該当なし |
| 前段成果物 | 0 | 該当なし |
| 既存合意の引き継ぎ | 0 | 該当なし |

**判定**: 上流入力は依頼者指示 1 件のみ

---

## 前提抽出

上流入力に内在する暗黙の前提：

1. **「sim Phase 9 段階 2」が基本設計書に定義済み**
   - Phase 分割のスコープ・技術的範囲が明示されていること
   - 段階 1 との分別が明確であること

2. **「strategy ブロック明示拒否」が既知の設計方針**
   - API レベルでの strategy・建玉変更への対応方法が定義されていること
   - 「拒否」の具体的意味（エラー応答など）が定義されていること

3. **ブランチ命名が GitFlow に準拠**
   - `feature/sim-phase9-stage2-api-sync` が feature ブランチとして許容されること
   - `backup/20260819-sim-phase9s2-pre` がバックアップ命名として許容されること

---

## 証拠先行検証

### 前提 1: sim Phase 9 段階 2 の基本設計書での定義

**検証手段**: `.doc/sim-backtest-ui-integration/基本設計書.md` を Read

**該当箇所**: 基本設計書 §19（Phase 9 設計）

**実証結果**:
```
§19.1: 段階 1 の範囲は front（`simulator/sim_ui/web/`）のみ。
       API・エンジン・Python 検定・保存済み spec は 1 byte も変えない。
       段階 2（sim API での `strategy`/建玉変更ブロック明示拒否）は別裁定。
```

**判定**: ✅ 前提成立。Phase 9 段階 2 は基本設計書に明記されている。

---

### 前提 2: strategy ブロック明示拒否が既知設計方針

**検証手段**: 基本設計書での関連記述確認

**該当箇所**: 基本設計書 §19.2 裁定

**実証結果**:
```
撤去（UI 出口のみ・機能は API で存続）: 買い/売り条件ビルダー・建玉変更（トレーリング/部分決済）
FR-05〜08（F-8/F-9）は API 面のみで提供
（§4.1 の機能一覧は有効のまま。UI 出口は本段階で撤去）
```

**判定**: ✅ 前提成立。strategy・建玉変更機能は「API で存続」との明示がある。「ブロック明示拒否」は別裁定(TBD)だが、方針はあり。

---

### 前提 3: ブランチ命名の妥当性

**検証手段**: `.doc/` および CLAUDE.md での GitFlow 命名規則確認

**該当箇所**: 既知の GitFlow 規則（feature/*, backup/*）

**実証結果**:
```
- feature/sim-phase9-stage2-api-sync: 
  feature ブランチ命名規則に準拠（feature/{scope}）
  
- backup/20260819-sim-phase9s2-pre:
  バックアップブランチ命名規則に準拠（backup/{YYYYMMDD}-{context}）
```

**判定**: ✅ 前提成立。命名は既知のGitFlow 命名規則に準拠。

---

## 判定結果

| # | 上流入力 | 前提 | 判定 | 理由 |
|---|---|---|---|---|
| 1 | sim Phase 9 段階 2 ブランチ準備（依頼者指示） | Phase 9 段階 2 の定義 | **採用** | 基本設計書 §19 に明記。スコープ明確。 |
| 1 | （同上） | strategy ブロック明示拒否の方針 | **採用** | 基本設計書 §19.2 に「API 面のみで提供」と記載。別裁定待ちだが方針は明確。 |
| 1 | （同上） | ブランチ命名の妥当性 | **採用** | GitFlow 既知命名規則に準拠。命名は妥当。 |

**最終判定**: 上流入力 1 件（依頼者指示）は全前提が成立。**採用**

---

## 残存リスク

本スキルの検証範囲外で後続作業に委ねるべき項目：

1. **段階 2 の詳細実装設計**
   - 「strategy ブロック明示拒否」の具体的な API レスポンス（400 エラー、403 禁止など）の定義
   - 既存の `/sim/strategy` エンドポイントへの影響範囲
   - **委ねる先**: 詳細設計フェーズ（作業ブランチでの実装着手時）

2. **API との整合性検査**
   - sim API 既存実装（`/sim/jobs` の `/strategy` ブロック読み込み）との整合確認
   - **委ねる先**: 統合テスト・API 検定フェーズ

なし以外はなし
