{
  "name": "indicator-ui-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Front pure-logic layer (DOM/chart/fetch independent). Tests run on node:test (node v24).",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'"
  }
}
