# 水準到達シート 基本設計書 v0.5.0

- 起票: 2026-08-28（ISSUE-449）／オシレータの価格投影は ISSUE-453
- 状態: **設計確定（実装未着手）**
- 依頼者裁定:
  - 到達時刻は定義 A（現在の到達が始まった時刻）で確定（2026-08-28 承認）
  - オシレータ系・ティックボリュームを**第 2 表**として追加（2026-08-28 承認）
  - **第 1 表を価格ラダーへ作り替える**（2026-08-29 承認・§4）
  - **水準は束ねない**。束ねると大枠の水準しか把握できない（2026-08-29 裁定・§4.1）
  - **チャート画面には置かない**。チャートは詳細分析の場として広く保つ（2026-08-29 裁定・§4.6）
  - **オシレータの分位を価格へ投影し、第 1 表の価格セルの背景として塗る**。対象は逆算可能な
    3 種すべて（2026-08-29 承認・§5.5）
  - **背景は地平 3 段で 3 分割する**。1 色は実測で色が定数化する（2026-08-29 承認・§5.5.5）

改訂履歴:

- **v0.5.0（2026-08-29）**: オシレータの分位水準を**価格へ投影**して第 1 表の価格セルへ
  ヒートマップとして反映する設計を §5.5 に追加した（ISSUE-453）。分位水準は**行ではなく
  価格軸上の区間の境界**として扱う。行として足す案は実測で破棄した（§10）。
  第 1 表の行構造・並び・列、第 2 表、各指標の core はいずれも**無改変**。
- **v0.4.0（2026-08-29）**: 第 1 表を「役割行 23 行 × 時間足 8 列」から**価格ラダー**へ作り替えた。
  時間足を列に置くと、価格という単一の比較軸が 8 列へ切り刻まれる（§4.2 に実測）。
  併せて地平（§4.3）・置き場所（§4.6）を確定し、**測定の誤り 2 件を是正**した（§11）。
- v0.3.0（2026-08-28）: 行定義を役割行 23 行へ確定。v0.2.0 の文言では表の 84.3% が空欄になる
  ことが作図で判明した。この表自体は v0.4.0 で第 1 表から外れたが、役割の考え方は
  ラダー各行のラベルとして残る。

---

## 1. 目的

各時間足で、**現在値が指標のどの水準に到達しているか**を直感的に把握する。表は 2 枚に分かれる。

| 表 | 何が到達するか | 縦の並び |
|---|---|---|
| **第 1 表: 価格ラダー** | 価格が、価格スケール上の水準に到達 | 価格降順の 1 本の軸（全指標・全時間足が価格という共通軸を持つ） |
| **第 2 表: オシレータ水準到達表** | 指標値が、自分の分位水準に到達 | 指標ごと（単位が異なり共通軸を持たない） |

2 表を分ける根拠: オシレータは指標ごとに単位が違い（% 乖離・RSI・tick 数）、第 1 表の価格順の並びに
混ぜられない。混ぜると 1 枚の中に読み方が 2 通り生じる。

**2 表を橋渡しする経路が 1 本だけある**（v0.5.0・§5.5）。オシレータの分位水準を**価格へ投影**し、
第 1 表の**価格セルの背景**として塗る。行は増えない（分位は行ではなく**区間の境界**である）。
読み方は 1 つに固定される: **この価格で引けたら、各地平の乖離は何段になるか**。

非目的（明示的に対象外）:

- 成績・エッジの評価（Sharpe・勝率・期待値）。本シートは現在状態の表示のみを行う。
- パラメータ最適化グリッド（パラメータ × パラメータ）。設計途中で検討したが破棄した（§10）。
- 指標の計算式・描画の変更。本シートは既存の計算結果を**読むだけ**である。

---

## 2. 用語定義（本書で固定する）

| 語 | 定義 |
|---|---|
| **水準** | 指標インスタンス（indicatorId ＋ params）が出力する水準系列 1 本。第 1 表では価格スケール上の線、第 2 表では指標自身の分位水準を指す。 |
| **観測値** | 水準と突き合わせる量。第 1 表では**価格**、第 2 表では**指標値**（% 乖離 / RSI / tick 数）。 |
| **到達** | 観測値がその水準を跨いだ状態。判定は固定値比較ではなく `value_t` と `level_t` の**交差**（水準自体がバーごとに動くため）。 |
| **到達時刻** | **定義 A**: 現在の到達状態が始まった時刻。観測値が水準を離れた時点でリセットされる。窓内の最古の到達（定義 B）は採らない。 |
| **chart 追従水準** | params の `timeframe` が `"chart"`（未指定含む）の水準。表示時間足ごとに値が異なる。 |
| **MTF 固定水準** | params の `timeframe` が `1D` / `1W` / `1M` 等で明示された水準。**表示時間足に依らず値が同一**。 |
| **足内更新** | ティックごとの更新。`/live_ticks` の tails 経路。 |
| **バー確定更新** | 足が閉じるたびの更新。`/compute` 経路。 |

---

## 3. 対象（実測に基づく確定列挙）

対象は「各時間足で実際に設定している指標」に限る。入力はチャートテンプレートの export
（`indicatorUi.templates.v1` / `indicatorUi.templateBindings.v1`）であり、カタログ全指標ではない。

### 3.1 第 1 表の対象（価格スケール＝価格が到達しうる水準を出す）

| indicatorId | 水準系列 | 足内更新 | 延べ本数 |
|---|---|---|---|
| `moving_averages` | `MA`（`Smoothing`/`Upper`/`Lower` は現行設定では未出力） | 可 | 57 |
| `btlm_trail` | `btlm_trail_mean` / `btlm_trail_q5` / `btlm_trail_q95` / `btlm_trail_off_hi` / `btlm_trail_off_lo` | 可 | 8 |
| `cvfe` | `cvfe_u1` / `cvfe_u2` / `cvfe_l1` / `cvfe_l2`（価格スケール上のバンド） | **不可** | 8 |

**`btlm_trail` は価格スケールに乗らない系列も返す**（`beta` −2〜218 / `sigma` 40〜5,591 /
`band_hit_rate` 0.70〜0.92）。これらは水準ではない。除外は名前ではなく**実値の桁**で判定する
（現在値の 0.3〜3 倍に入るか）。実測でこの 21 本を水準として数えていた誤りがあった（§11）。

### 3.2 第 2 表の対象（オシレータ＝指標値が自分の分位水準に到達する）

| indicatorId | 到達する量 | 水準系列 | 足内更新 | 延べ本数 |
|---|---|---|---|---|
| `ma_marod` | MA 乖離率（%） | 分位帯 `_q{pct}` ＋ イベント分位 `_evq_{med\|ext}_{hi\|lo}` | 可 | 9 |
| `btlm_trail_marod` | トレンド乖離率（%） | 同上 | 可 | 8 |
| `profit_rsi` | RSI（0–100） | 正常帯 `rsi_q{pct}` ＋ `rsi_evq_ext_{hi\|lo}` ＋ GPD 外挿 | 可 | 8 |
| `tickvol` | tick 数 | 正常帯 `tickvol_q{pct}` ＋ 経験的水準 ＋ GPD 水準 | 可 | 8 |

4 種すべてが増分器を宣言済みで、**足内更新できる**（第 1 表の `cvfe` のような更新粒度の差が無い）。

### 3.3 対象外と理由

| indicatorId | 理由 |
|---|---|
| `market_profile` | 価格分布そのもの。水準の概念が別（POC・VA）。追加要否は未裁定（§9） |
| `price_range_power` | 価格軸分布（`archetype = axis_distribution`）。同上。4h に 1 本のみ |

### 3.4 時間足と紐付け（両表に共通）

`1m`→tpl#4 / `5m`→tpl#5 / `15m`→tpl#6 / `1h`→tpl#7 / `4h`→tpl#3 / `1D`→tpl#9 / `1W`→tpl#8 / `1M`→tpl#4。

- `1m` と `1M` が同一テンプレート `tpl#4` を共有するのは**正しい設定**（依頼者確認済み 2026-08-28）。
- `tpl#2`（名前 "D"）は未紐付け。**未確認事項**（§9）。

---

## 4. 第 1 表 = 価格ラダー（v0.4.0 で確定）

### 4.1 形

**価格を唯一の軸**にする。全時間足の価格水準を 1 本の価格軸へ投影し、価格降順に並べる。
時間足は軸ではなく**各行の属性**（バッジ）になる。

- **行** = 水準 1 本（指標インスタンス × 系列）。**束ねない**（依頼者裁定 2026-08-29）。
- **列** = 距離 / 価格 / 時間足 / 水準ラベル の 4 列。時間足の列は持たない。
- **並び** = 価格降順。現在値は独立行として価格順の位置に入る。
- **各行** = 現在値からの距離、水準の実数値、**直前行との価格差**、時間足、一意なラベル。

実測（2026-08-29・ライブ計算サーバ・JP225）: **価格水準 88 本**（現在値 65,756.0 で上 44 / 下 44）。

束ねない根拠（依頼者裁定）: 束ねると大枠の水準しか把握できない。実測上の損失も無い
（§4.4 のとおり帯幅を変えても「次のターゲット」の持続は伸びない）。重なりは
**直前行との価格差**を各行に添えて読み取れるようにする。

### 4.2 なぜ時間足を列にしてはならないか（実測）

価格は全時間足で**同じ 1 点**である。一方、水準の値は 88 個あり、すべて同じ価格スケールに乗る。
比較すべき軸は**価格 1 本だけ**で、時間足は比較の軸ではなく水準の出所にすぎない。

実測: 現在値のすぐ上に来る水準は `5m` → `1m` → `5m` → `1m` → `5m` → `15m` と**列をまたいで
交互に現れる**。行でも列でも一直線に読めず、**88 セルを総当たりで見比べるしかない**。
ラダーでは同じ問いが**現在値の隣接行**で決まる。

v0.2.0〜v0.3.0 で検討した「役割行 23 行 × 時間足 8 列」の表は、この理由により第 1 表としては採らない。
（役割の考え方はラダー各行のラベルとして生きる。詳細は §10。）

### 4.3 地平（horizon）— 「次のターゲット」は 1 つではない

「次のターゲット」＝ 現在値より上（下）で最も近い水準。これは**どの時間足の水準を数えるかで変わる**。

実測（1m 標本 390 本＝6.5 時間）:

| 含める水準 | 上 持続 | 上 距離 | 下 持続 | 下 距離 | 水準数 |
|---|---|---|---|---|---|
| すべて（1m 以上） | 1.5 分 | 10 点 | 1.5 分 | 10 点 | 88 |
| 5m 以上 | 2.1 分 | 16 点 | 2.0 分 | 17 点 | 78 |
| 15m 以上 | 3.7 分 | 54 点 | 3.9 分 | 39 点 | 68 |
| **1h 以上** | 8.3 分 | 127 点 | 6.6 分 | 166 点 | 58 |
| 4h 以上 | 16.2 分 | 140 点 | 10.2 分 | 176 点 | 46 |
| **1D 以上** | 77.8 分 | 342 点 | 21.6 分 | 209 点 | 34 |
| 1W 以上 | 389 分 | 1,834 点 | 29.9 分 | 245 点 | 18 |

距離・持続とも単調に増え、段が重ならない。**短期＝すべて / 中期＝1h 以上 / 長期＝1D 以上**の
3 段の直上・直下に「次」の印を付ける。

**持続は指名の安定性であって、当たるかどうかとは無関係である**（§9-1）。

### 4.4 束ねる案を保留にした記録

近接した水準を帯に束ねる案を検討したが採らない。測って分かったことだけ残す。

- 束ねても**「次のターゲット」の持続は伸びない**（帯幅 5 → 66 で 1.4 → 1.3 分と横ばい）。
  利点は行数が減ることだけだった。
- 「近ければ束ねる」と単純に連結すると**鎖状につながって 22 本・幅 252 点へ膨張**する。
  帯の**幅そのものに上限**を課さないと使えない。
- 帯は**現在値をまたぐ**（実測 1 件）。上下 2 分類で扱うと最も近い水準が表から消える。
  上 / 現在値を含む帯 / 下 の 3 区分が要る。
- 束ねない現行案では、この 2 つの問題はどちらも起きない。

### 4.5 指名が変わる原因（実測）

水準そのものはほとんど動かない（1m あたりの移動は中央 0.00 点。動くのは `1m` 水準だけで
中央 2.37 点）。一方、価格は 1 分で中央 12.9 点動く。

| 交代の原因 | 短期 | 中期 |
|---|---|---|
| 価格が前の目標を上抜けた | 46% | 40% |
| **前の目標は上に残ったまま、別の水準が割り込んだ** | **54%** | **60%** |

割り込む水準は短期で `1m`×64・`5m`×51 が大半。**短い足の水準が動いて目標を追い越している**。
これは価格の前進ではなく目標側の揺れであり、ISSUE-451（板注文）では発注管理の仕様に直結する。

### 4.6 置き場所 = ダッシュボード（ISSUE-452）

**チャート画面には置かない**（依頼者裁定 2026-08-29: チャートは詳細分析の場であり画面を広く保つ）。
`/live` `/replay` `/sim` と並ぶ 4 つ目のモード `/dashboard` に置く。詳細は ISSUE-452。

実測で却下した置き場所:

| 案 | 判定 | 根拠（実測 2026-08-29・実 UI） |
|---|---|---|
| チャートの価格軸へ整列 | 不可 | 価格ペインは **210px**（4 ペイン構成 210/70/70/69＋時間軸 28）。88 水準で **2.4px/行** |
| チャート画面内のページ級タブ | 不可 | ページ級タブは存在せず（タブは指標追加ダイアログ内のみ）、導入すると中央 factory が要る＝OCP 違反 |
| チャート右の併置パネル | 不可 | チャート幅が約 320px 狭くなる（依頼者裁定で却下） |

### 4.7 版面イメージ（実測値・抜粋）

```
    距離        価格            時間足   水準
  +343.7   66,099.7            1D      MA ema5 hlc3        ← 長期・上
  +115.6   65,871.6            4h      MA ema31 low        ← 中期・上
   +47.4   65,803.4  差 16.2   5m      cvfe 外側上 2σ
   +32.5   65,788.5  差 14.9   1m      cvfe 外側上 2σ
   +24.0   65,780.0  差  8.6   5m      MA ema24 high
   +22.3   65,778.3  差  1.6   1m      cvfe 内側上 1σ
   +14.7   65,770.7  差  7.6   5m      cvfe 内側上 1σ      ← 短期・上
━━ 現在値 65,756.0 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    -1.5   65,754.5  差 16.3   1m      MA ema60 high       ← 短期・下
    -4.4   65,751.6  差  2.9   5m      MA ema24 hlc3
   -14.0   65,742.0  差  9.6   1m      MA ema60 close
   -18.5   65,737.5  差  4.5   1m      cvfe 内側下 1σ
   -20.2   65,735.8  差  1.7   15m     btlm_trail q5
  -209.1   65,546.9            1W      MA ema13 hlc3       ← 中期・下／長期・下
```

「差」= 直前行との価格差。小さい値が続く所ほど水準が重なっている
（実測: 65,720〜65,738 の 18 点幅に 6 本）。

---

## 5. 第 2 表（オシレータ水準到達表）の構造と、分位の価格投影

### 5.1 行と列

- **行** = 指標インスタンス（`ma_marod` / `btlm_trail_marod` / `profit_rsi` / `tickvol`）。
- **列** = 表示時間足 8 列。第 1 表と違い **MTF 共通列は無い**（現行設定に `timeframe` 明示の
  オシレータ instance が無いため）。
- **セル数** = 延べ 33（`ma_marod` のみ 1D に 2 本）。

### 5.2 セルの内容

| 要素 | 内容 |
|---|---|
| 到達段 | 帯内 / 上帯超 / 下帯超 / ext 超 / GPD 超（配色で表現） |
| 現在値 | その指標のその時間足での現在値（単位は指標ごと: % / 0–100 / tick 数） |
| 到達時刻 | 到達済みのときのみ。定義 A（§6.2）を第 1 表と同一に適用 |

第 1 表と違い、現在値は指標ごと・時間足ごとに異なる。よって**現在値の独立行は流用できず**、
各セルが自分の現在値を持つ。

### 5.3 到達段の順序（配色の基準）

```
GPD 超 ← ext 超 ← 上帯超 ← 帯内 → 下帯超 → ext 超 → GPD 超
（上側ほど過熱・下側ほど沈静。帯内が中立）
```

`tickvol` は計数量で下側が裾でないため **上側のみ**（`levels.py` ③「上側のみ」）。下側の段は持たない。

### 5.4 版面イメージ

```
                     1m        5m       15m       1h       4h       1D       1W       1M
ma_marod            帯内      帯内     上帯超    上帯超   ext超    ext超    帯内     帯内
                    +0.8%     +1.2%    +2.1%     +2.4%    +3.9%    +4.2%    +1.1%    +0.4%
                     ―         ―       09:12     09:12    昨日     3日前     ―        ―
btlm_trail_marod    帯内      上帯超   上帯超    ext超    ext超    GPD超    上帯超   帯内
profit_rsi          帯内      帯内     帯内      上帯超   上帯超   ext超    帯内     帯内
tickvol             上帯超    ext超    ext超     上帯超   帯内     帯内     帯内     帯内
```

### 5.5 オシレータ分位の価格への投影（第 1 表への反映・v0.5.0・ISSUE-453）

第 1 表と第 2 表は単位が違うため分離されている（§1）。そのため「価格がこの水準へ来たとき、
乖離や RSI はどれほど過熱しているか」を 1 枚で読めない。本節はその橋を架ける。

**分位水準は行ではなく、価格軸を区切る境界である。** 第 1 表の行構造・並び・列は変えず、
各行の**価格セルに背景色**が付くだけである。読み方は 1 つに固定する:

> **この価格で引けたら、各地平の乖離は何段になるか。**

#### 5.5.1 対象（逆算可能性で決まる。列挙ではない）

| indicatorId | 到達する量 | 逆算 | instance | 水準 延べ |
|---|---|---|---|---|
| `ma_marod` | MA 乖離率（%） | 可 | 9 | 54 |
| `btlm_trail_marod` | トレンド乖離率（%） | 可 | 8 | 48 |
| `profit_rsi` | RSI（0–100） | 可 | 8 | 43 |
| `tickvol` | tick 数 | **不可**（価格の関数でない） | 8 | 39 |

`tickvol` の除外は**列挙で書かない**。§5.5.3 の `breakpoints()` を提供できないという形で
構造に現れる（LSP・§8）。

#### 5.5.2 前進評価の形（実測 2026-08-29・`probe_inverse.py`）

各指標の core を**無改変で前進評価**し、現在バーの終値 `C` に対する指標値 `v(C)` を測った。
逆関数の数式は書いていない（参照実装があるものを推測で実装しない）。

**`v(C)` は区分メビウス `(aC + b) / (C + d)` である。**

| 指標 | 区分数 | 3 点当てはめの残差（最大） | 単調性 |
|---|---|---|---|
| `ma_marod` | 3 | 6.0e-14 | 増加 |
| `btlm_trail_marod` | 3 | 6.0e-14 | 増加 |
| `profit_rsi` | 6 | 3.9e-12 | 増加 |

区分の境目は**適用価格 hlc3 の折れ**（走行 H / L。`C` が高値を越えれば高値も動く）。
`profit_rsi` はさらに**上下分岐**（`x_t` が `x_{t-1}` を跨ぐ点）が加わる。

ここから 2 つが確定する。

- **逆写像は近似ではなく閉形式で厳密**である: `C = (b − v·d) / (v − a)`。
  反復探索・ニュートン法・二分法はいずれも要らない。
- **全区分で単調増加**である。よって価格の交差による到達判定が指標値の交差と**同値**になり、
  §6.1 の判定規約をそのまま適用できる。**判定を二重に持たない。**

#### 5.5.3 構造（指標側が実装するのは境目だけ）

```
BreakpointSource（指標ごと）      : breakpoints() -> [価格]        ← これ 1 メソッドのみ
PriceValueMap（指標を知らない）   : fit(forward, breakpoints)      ← 区分ごとに 3 点で係数を決める
                                   value_at(price) / price_at(value)
```

- `forward(C) -> value` は**既存の増分器をそのまま呼ぶ**。指標の core は 1 行も変えない。
- **逆関数の数式を指標ごとに手書きしない**。逆写像はメビウス当てはめ 1 か所が所有する
  （同じ計算の第 2 定義を作らない・§8「単一ソース」）。

#### 5.5.4 費用（実測 2026-08-29・`probe_heatmap.py`）

`v(C)` がメビウスなので、**係数を決めた後の価格評価は前進評価を一切呼ばない**。

```
係数決定             : instance あたり 区分数 × 3 回（marod 9 回 / RSI 18 回）
ラダー 82 行の評価   : 発行 0 回       （参照実装を直接呼ぶなら 2,050 回）
閉形式と直接呼び出しの最大差 : 1.1e-08
```

係数は**現在の価格 `C` に依存しない**（前バーの状態と走行 H / L だけで決まる）。よって
再計算の契機は**バー確定と走行極値の更新のときだけ**であり、ティックごとではない（§7）。

#### 5.5.5 塗る単位 = 地平 3 段（2026-08-29 承認・**3 分割で確定**）

価格セルの背景は**地平 3 段（短期＝すべて / 中期＝1h 以上 / 長期＝1D 以上・§4.3）で 3 分割**する。
§4.3 と同じ区分なので新しい概念が増えない（認知負荷の最小化）。段の順序は §5.3 をそのまま使う。

**1 色（全 instance の最も極端な段）は実測で破綻するため採らない。**

| 案 | 実測（ラダー 82 行 × instance 25 本） |
|---|---|
| 1 色 | `+3` が 60 行・`−3` が 22 行 ＝ **|段| = 3 が 82/82 行（100%）**。**色が定数**になり何も区別しない |
| **3 分割（採用）** | 3 段の値が**食い違う行が 71/82（87%）**。1 色にするとこの情報が消える |

段の分布（3 分割・82 行）:

| 地平 | −3 | −2 | −1 | 0 | +1 | +2 | +3 |
|---|---|---|---|---|---|---|---|
| 短期 | 22 | 7 | 5 | 12 | 2 | 19 | 15 |
| 中期 | 11 | 7 | 33 | 21 | 0 | 6 | 4 |
| 長期 | 7 | 0 | 0 | 0 | 0 | 0 | 75 |

1 色が潰れる原因は、長期のオシレータが常に最極端で `max` がそれを全行へ伝播させることにある。

**長期の列はラダーのレンジ内でほとんど動かない**（`+3` 75 行・`−3` 7 行の 2 値のみ）。
ラダーの幅 ±350 点 ＝ **0.53%** に対し、長期の帯は **±5〜20%** だからである。実質的に効くのは
短期と中期の 2 列で、長期は「今どちら側にいるか」の 1 ビットとして持つ。
**これを理由に長期を落とさない**（§4.3 の地平 3 段と食い違うため・依頼者裁定 2026-08-29）。

#### 5.5.6 版面イメージ

```
    距離        価格   差   時間足  水準                 短 中 長
  +343.7   66,099.7        1D    MA ema5 hlc3         ░ ░ ▓   ← 長期・上
  +115.6   65,871.6        4h    MA ema31 low         ░ ▒ ▓   ← 中期・上
   +47.4   65,803.4  16.2  5m    cvfe 外側上 2σ       ░ ▒ ▓
   +32.5   65,788.5  14.9  1m    cvfe 外側上 2σ       ░ ▒ ▓
━━ 現在値 65,756.0 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    -1.5   65,754.5  16.3  1m    MA ema60 high        ░ ▒ ▓   ← 短期・下
   -32.8   65,723.2   2.3  1m    btlm_trail mean      ▒ ▒ ▓
  -209.1   65,546.9        1W    MA ema13 hlc3        ▓ ▒ ▓   ← 中期・下／長期・下
```

「短 / 中 / 長」＝ その価格で引けたときの各地平の段（§5.3 の順序で配色）。

#### 5.5.7 第 2 表は廃止しない

第 2 表は「**今**どの段にいるか」、本節は「**どの価格で**どの段になるか」であり、問いが違う。
片方が他方を含まない。

---

## 6. 到達判定と到達時刻の導出（両表に共通）

### 6.1 判定（交差）

水準はバーごとに動くため、固定値との比較では判定できない。バー系列 `t` に対し

```
reached_t := (value_t >= level_t)      # 上側水準は方向を反転
                                       # value = 第1表なら価格・第2表なら指標値（§2 観測値）
```

現在の到達状態は最新点の `reached` である。

### 6.2 到達時刻（定義 A）

現在の到達状態が始まった時刻 = **`reached` が現在と同値である連続区間の始端の時刻**。

```
first_t := min{ s | reached_s = reached_now  かつ  ∀u∈[s, now] : reached_u = reached_now }
```

観測値が水準を離れて戻ると、戻った時点が新しい `first_t` になる。

### 6.3 クライアント側で積み上げてはならない（根本要件）

到達時刻をフロントで観測しながら積み上げると、**シートを開いた時刻に依存**し、開き直すたびに値が変わる。
これは表示の欠陥であり、選択肢ではない。到達時刻は必ず**水準系列と観測値系列の履歴の突合**から導出する。
履歴は既存 `full_compute`（`adapter/compute/latest_dispatch.py:27-31`）が返す全件系列で足りる。
新規の統計・新規の保存領域は不要。

---

## 7. 更新経路（2 段）

| 段 | 契機 | 経路 | 内容 |
|---|---|---|---|
| 1 | バー確定 | `/compute`（全件系列） | 水準系列と観測値系列を突合し、全セルの水準値と到達時刻を再導出する |
| 2 | ティック | `/live_ticks` の tails | 観測値を更新し、到達状態（第 1 表は行の価格降順の並びも）を更新する |

段 2 の対象は足内更新を宣言済みの指標に限る。`cvfe` は増分器が無いため段 1 のみで更新される
（`adapter/compute/live_tick_tails.py` が `None` を返し、ティック更新から明示的に落ちる）。
これを隠さず、`cvfe` 行には更新粒度がバー確定であることを表示する。

第 2 表の 4 種はすべて増分器を宣言済みで、段 2 で更新される（更新粒度の差は生じない）。

**§5.5 の価格投影（背景色）の更新契機は 3 段目ではない。** メビウス係数は現在の価格に依存せず、
前バーの状態と走行 H / L だけで決まる（§5.5.4）。したがって:

| 契機 | 係数の再当てはめ | 背景色の再計算 |
|---|---|---|
| バー確定 | **要**（instance あたり 区分数 × 3 回の前進評価） | 要 |
| 走行 H / L が更新された | **要**（同上） | 要 |
| 上記以外のティック | **不要（0 回）** | 要（閉形式のみ。前進評価は発行しない） |
| ラダーの行が増減した | **不要（0 回）** | 要（閉形式のみ） |

費用: 既存記録の実測は 0.5〜0.86 ms/tick/インスタンス（ISSUE-257）。段 2 の対象は第 1 表 88 水準・
第 2 表 33 セルの合計より少ないが、**着手前に現行コードで再実測する**（記録値をそのまま設計根拠にしない）。

段 1 の実測（2026-08-29・warm・`market_profile` 除く）: 8 時間足ぶんの価格水準（3 指標・71 本）で
**2,316ms**、同じ 8 時間足の全指標（105 本）で **3,637ms**。ラダーの 71 本は全指標 105 本の
**部分集合**であり、別々に計算すると 2,316ms が丸ごと無駄になる（ISSUE-450 と同型）。

**計算量テスト（CLAUDE.md 絶対命令 §4.1・実装時に必ず添える）**: 本シートは既存系列を読むだけであり、
新規の計算を発行してはならない。よって Test Spy で「シートが発行した compute 呼び出し数 −
既にチャートが発行している呼び出し数 = 0」を表明する。加えて**行数・列数を増やしても発行が増えない**
ことを 2 点以上（例: 11 行 / 23 行）で固定する。回数そのものは期待値に焼き込まない。

**§5.5 の価格投影に固有の計算量テスト**: `forward` を Test Spy で数え、次を表明する。
回数そのもの（9 や 18）は**期待値に焼き込まない**。固定するのは無駄の不在である。

- **無駄の不在**: 発行した前進評価 − 係数決定に使った前進評価 = **0**。
- **オーダーの表明（行数）**: ラダー行数 82 → 164 で発行回数が**変わらない**（2 点固定）。
- **オーダーの表明（水準数）**: 1 instance の水準 6 本 → 12 本で発行回数が**変わらない**。
- **オーダーの表明（ティック）**: バーが確定せず走行 H / L も更新されないティックでの発行が **0**。

「行として足す案」（§10）はこの 4 つを 1 つも満たせない（行数・水準数の両方に比例するため）。

---

## 8. 設計方針（SOLID 写像）

- **OCP**: 既存の compute・指標定義・チャート UI を改変しない。シートは既存の計算結果を読む追加の購読者。
  §5.5 の価格投影も同じで、各指標の core・第 1 表の行構造・第 2 表はいずれも**無改変**。
  追加は「価格 → 段」を返す問い合わせ口 1 つだけである。
- **単一ソース**: セルの計算式を写さない。水準＝既存系列、到達＝既存系列同士の比較。計算の第 2 定義を作らない。
  §5.5 では**逆関数の数式を指標ごとに手書きしない**。逆写像は `PriceValueMap` 1 か所が所有する。
- **DIP**: 交差判定・到達時刻導出・行の並び替えは純関数（DOM・HTTP・localStorage 非依存）。
  §5.5 の当てはめは既存の増分器を `forward(C) -> value` として抽象越しに呼び、具象指標へ依存しない。
- **ISP**: 既存の TailSpec 契約（`instanceId` / `indicatorId` / `variant` / `params`）をそのまま使い、広げない。
  §5.5 で指標側が実装するのは `breakpoints()` **1 メソッドのみ**（逆関数の数式は要求しない）。
- **SRP**: `PriceValueMap`（当てはめと逆写像）と `BreakpointSource`（区分の境目）を分ける。
  前者は指標を知らず、後者は数式を知らない。
- **LSP**: §5.5 の 3 指標は同一契約を満たす。`tickvol` は `breakpoints()` を提供できず、
  **対象外が構造で表れる**（列挙による除外を書かない）。

---

## 9. 未確定事項（着手前に埋める）

1. **測定済み（2026-08-29・§13）**。到達率・到達までの時間・反応を 52 日で実測した。
   結論: **到達率と到達時間は距離で素直に決まるが、反応には水準としての情報が無い**
   （同じ距離の「水準でない価格」と有意差なし）。詳細は §13。
2. **地平の段の切り方**（3 段でよいか・境界を 1h / 1D にするか）。根拠は **6.5 時間・1 銘柄・
   1 回の観測**のみ。長期は標本 6 件しかない。**日をまたいだ再測が要る**。
3. `market_profile`（POC・VA）と `price_range_power`（価格帯）を水準として追加するか。
   どちらも価格水準でありラダーの定義に合致するが、依頼者指定が無いため対象外にしている。
4. 段 2 の 1 ティックあたり費用の現行コードでの再実測。
5. 到達時刻の表示粒度（当日は時刻・過去日は相対表記、の境界規則）。
6. `tpl#2`（名前 "D"）が未紐付けである理由。設定側の意図確認が必要（本設計では変更しない）。
7. **§5.5 未測定**: 段の**時間的な安定性**。§4.3 の「指名の持続」に相当する量を段については
   測っていない。表示に添えるなら着手前に実測する。**断定しない。**
8. **§5.5 未測定**: 走行 H / L の更新頻度（＝メビウス係数の再当てはめ頻度）。
   1m での更新率が段 2 の費用を決める。着手前に実測する。
9. **設定側の不整合（本設計では変更しない・依頼者判断）**: `profit_rsi` の `5m` と `15m` の設定に、
   現在の `add_rsi` に存在しないパラメータ `ma_period` が入っており、水準パラメータ
   （`window_n` / `q_low` / `q_high` / `q_out` / `k_events`）が未指定＝既定値で動作している。
   第 2 表のこの 2 セルだけ、水準が意図した設定になっていない。

---

## 10. 破棄した案と理由

| 案 | 破棄理由 |
|---|---|
| パラメータ × パラメータのグリッド 65 枚 | 時間足を軸に置かないと「各時間足が」を比較できない。目的に対して構造が合わない |
| 分位水準に限定した 5 指標のシート | 「分位水準」は依頼者の用語誤りで、真の要件は「観測値が到達した水準」 |
| **役割行 23 行 × 時間足 8 列の表**（v0.3.0 の第 1 表） | 価格という**単一の比較軸を 8 列へ切り刻む**。現在値の直上に来る水準は列をまたいで交互に現れ、88 セルの総当たりが要る（§4.2）。役割の考え方はラダー各行のラベルとして残す |
| 近接水準を帯に束ねる | 大枠の水準しか把握できない（依頼者裁定）。実測上も持続は伸びない（§4.4） |
| オシレータを第 1 表へ混ぜる | 単位が指標ごとに違い共通の縦軸を持たない。1 枚に読み方が 2 通り生じるため第 2 表として分離した |
| 足内更新できない指標をバー確定更新で同一に扱い「リアルタイム」と称する | 症状回避の応急処置。更新粒度が違うことを隠さず表示する（§7） |
| ラダーをチャート画面に置く（価格軸整列・タブ・併置パネル） | §4.6 の実測により 3 案とも不可。ダッシュボード（ISSUE-452）へ置く |
| **オシレータの分位を価格へ変換して「ラダーの行として足す」** | 変換後の水準 **145 本**の現在値からの距離は**中央 3,252 点**で、ラダーの実効レンジ ±350 点に入るのは **17 本（11.7%）**しかない。表を ±30,000 点まで伸ばすと既存 82 本が全幅の 1% 未満へ潰れる。原因は表示の工夫不足ではなく、**上位足の分位がもともと価格として遠い**こと（1M の `ma_marod_q95` = +18.96% 乖離）。加えて `profit_rsi` は逆写像が**極**を持ち、現在バーで到達しうる上限へ近づくと価格が発散する（1W `rsi_evq_ext_hi` → 345,009）。境界として扱う案（§5.5）へ置き換えた |
| 変換後の水準を**距離で足切り**してラダーへ足す | 症状の出る条件（遠い水準）を避けているだけで、原因（分位が価格として遠いこと）を消していない＝**応急処置**。CLAUDE.md 禁止事項に該当する |
| 価格セルの背景を**1 色**（全 instance の最も極端な段）にする | 82/82 行（100%）が \|段\| = 3 になり**色が定数化**する。長期のオシレータが常に最極端で `max` が全行へ伝播するため（§5.5.5） |
| 逆写像を指標ごとに手書きする | 3 種とも `v(C)` が区分メビウスであり（§5.5.2）、当てはめ 1 か所で足りる。手書きは同じ計算の第 2 定義を 3 つ作ることになり、取り残しを生む |

---

## 11. 測定の誤りと是正（記録）

設計中に自分の測定へ 2 件の誤りがあった。結論は変わらなかったが記録する。

1. **価格でない系列を水準として数えていた**。`btlm_trail` の `beta` / `sigma` / `band_hit_rate`
   の **21 本**を含めて「水準 109 本」と報告していた。正しくは **88 本**。3 系列とも現在値から
   遠く、現在値まわりの結論（次のターゲット・地平）は変わらなかったが、本数と下側の数は誤りだった。
   是正: 除外は名前ではなく**実値の桁**で判定する（§3.1）。
2. **行の識別子が衝突していた**。`MA` は各時間足に 3〜9 本あるのに「系列名＋時間足」を識別子に
   していたため、88 本が 68 本に潰れて安定性の測定が狂っていた。是正: パラメータまで含めて
   `MA ema24 hlc3 · 5m` の形にし、**88 行すべてが一意**であることを検査する。

併せて、**「目標として使える」という判断を基準を定義せずに述べた**ことを撤回した（§9-1）。

---

## 12. 関連

ISSUE-449（本件）・**ISSUE-453（オシレータ分位の価格投影＝§5.5）**・
**ISSUE-452（ダッシュボード＝置き場所）**・**ISSUE-451（板注文）**・
ISSUE-450（起動と操作の費用・同型の「作ってから捨てる」欠陥）・ISSUE-257（末尾値の費用と仕事の量）・
ISSUE-368（銘柄仕様・桁の単一ソース）・ISSUE-233 / ISSUE-262（増分器の宣言と無言縮退）・
`.doc/MTF_INDICATOR_PROJECTION_BASIC_DESIGN.md`（MTF 投影）。

---

## 13. 到達率・到達までの時間・反応の実測（2026-08-29）

素材: JP225・1m 足 50,000 本（**52.1 日**・2026-07-07 〜 2026-08-28 UTC）・価格水準 82 本。

### 13.1 定義（本測定で固定する）

| 語 | 定義 |
|---|---|
| 標本 | **60 分ごと**の 1m 足（834 点）。価格は 1m close |
| 指名 | その地平で、価格より上（下）にある最も近い水準の値 `v` |
| 到達 | `v` を**固定した目標**として、以後 K 分以内に 1m high ≥ `v`（上）/ low ≤ `v`（下）。固定するのは指値を発注時の価格に置くのと対応させるため |
| 反応 | 到達後、`v` から X 点**戻る**のと X 点**進む**のどちらが先か。戻るが先＝反転 |
| 対照 | **別の時刻・同じ距離**で、どの水準からも 15 点以上離れた価格。距離を厳密に揃え、水準性だけを外す |

### 13.2 到達率・到達までの時間

| 地平 | 向き | 件数 | 距離（中央） | 到達率 | 到達まで（中央） | 観測窓 |
|---|---|---|---|---|---|---|
| 短期（全部） | 上 / 下 | 834 / 834 | 11 点 / 11 点 | 95% / 95% | 1 分 / 1 分 | 120 分 |
| 中期（1h 以上） | 上 / 下 | 834 / 834 | 95 点 / 87 点 | 86% / 86% | 16 分 / 16 分 | 480 分 |
| 長期（1D 以上） | 上 / 下 | 834 / 834 | 304 点 / 277 点 | 78% / 84% | 119 分 / 122 分 | 2880 分 |

**これは使える**。「その価格までどれくらいか」の見積もりになる。ただし**距離で決まる量**であり、
水準であることの効果ではない（同じ距離の任意の価格でも同じになる）。

### 13.3 反応 — 水準に情報は無かった

| 地平 | 向き | 水準 反転率 | 対照 反転率 | 差 | z |
|---|---|---|---|---|---|
| 短期 | 上 / 下 | 77% / 79% | 81% / 83% | −3.5 / −5.0pt | −1.7 / −2.5 |
| 中期 | 上 / 下 | 87% / 90% | 89% / 90% | −1.8 / +0.3pt | −1.0 / +0.2 |
| 長期 | 上 / 下 | 89% / 92% | 91% / 95% | −2.4 / −2.6pt | −1.5 / −1.9 |

反応幅を 30 点にしても差は −5.4 〜 +2.8pt（|z| ≤ 2.3）で符号が一定しない。
**12 比較で |z| ≤ 2.5 は多重性を考えれば有意でない**（Bonferroni の閾値は約 2.9）。

> **結論: 水準に到達しても、同じ距離の「水準でない価格」に到達した場合と反応は変わらない。**
> 「水準に到達したら反発する」という前提は、この測定では支持されない。差があるとすれば
> むしろ**水準のほうがわずかに反転しにくい**方向だが、これも有意でない。

### 13.4 この結論が効く範囲

- **本シート（表示）には影響しない。** ラダーは「価格がいまどこにあるか」を示すものであり、
  反発を予言していない。到達率・到達時間（§13.2）は表示に添える情報として使える。
- **ISSUE-451（板注文）には決定的**。「水準に指値を置けば有利」を前提にできない。
  発注の根拠は別に要る。

### 13.5 第 1 版の欠陥（記録）

最初の測定は到達率 88〜96% を出したが、**エピソード始端だけを標本にした偏り**だった。
新しい目標は「価格が直前の目標を通過した直後」に現れるため、始端は価格のすぐ近くに偏る
（中期の距離が、全標本 127 点に対し始端 35 点）。周期標本へ変えて是正した。
対照も当初は `v ± (20〜60)` 点にずらしており距離が揃っていなかったため、
**別の時刻・同じ距離**へ変更した。

### 13.6 限界（この結論を超えて一般化しない）

- 1 銘柄（JP225）・52 日・現行テンプレート 1 設定のみ。
- 反応の定義は「先に X 点戻るか進むか」（X = 15 / 30）。他の定義（滞留時間・出来高反応）は未測定。
- 到達率は観測窓 K に依存する。K は地平の持続に合わせて選んだ（120 / 480 / 2880 分）。
