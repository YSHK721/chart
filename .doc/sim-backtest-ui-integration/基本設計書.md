# simulator バックテストのライブ / リプレイ統合 UI 統合（第 3 モード「シミュレーション」）基本設計書

> 対象機能: unified_ui（公開ポート 8000・ライブ / リプレイ統合）に第 3 モード
> 「シミュレーション」を追加し、(1) ポジションサイズ計算機のサイジング規則、
> (2) インジケーター駆動戦略によるバックテストの実行指示、(3) 結果閲覧（report_ui の
> 完全再現）を統合する最上流設計。
> 口座規約の権威は `simulator/usecase/account_engine.py`（ISSUE-369 RESOLVED）。
> 表示の移植元の正は `simulator/report_ui/`。統合機構の前例は
> `.doc/LIVE_REPLAY_UNIFICATION_BASIC_DESIGN.md`（ルータ方式）。

---

## 1. 文書情報

- 作成日：2026-08-11
- バージョン：v0.1.0（基本設計・レビュー前）
- 作成者：system-basic-design エージェント
- 承認者：（未承認・本書 §9.3 の TBD-01 / TBD-02 を解消後にレビューへ）
- 変更履歴：v0.1.0 初版

### 1.1 本書における上流入力の取り扱い（証拠先行）

本書は依頼者の確定済み裁定を上流入力として受領した。`upstream-input-validation` に従い、
各裁定に内在する事実主張を実コード grep / Read で検証した結果を §10.5 に記載する。
検証の結果、**上流入力 1 件が実測により反証され（v5.2.0 のマーカー hover 検出は既に実証済）**、
**上流入力 2 件の間に相互不成立の関係が確定した（StrategyPort 境界 と run_backtest.py 無改変）**。
前者は本書で訂正して採用し（§3.5.2）、後者は依頼者裁定を要する未確定事項として
§9.3 TBD-01 に記録する（設計側で一方的に決定しない）。

---

## 2. プロジェクト概要

> 出力元：S-1 要件分析と設計方針決定

### 2.1 システム概要

- **位置付け**：`unified_ui` の公開 8000 上に、ライブ・リプレイと並ぶ第 3 のモード
  「シミュレーション」を追加する。本モードは「バックテストの実行を指示し、その結果を閲覧する」
  操作面を担う。バックテストの計算そのものは新設の sim コアサーバが子プロセスのジョブとして実行する。
- **解決する業務課題**：現状、戦略の検証操作は 3 つの独立した場所に分断されている。
  1. サイジング規則の検討＝単体 HTML の計算機（`integrated_position_sizing_calculator.html`・ISSUE-370）。
  2. バックテストの実行＝CLI（`simulator/main/__main__.py:23-33` の `argparse` 経路）。
  3. 結果の閲覧＝別ポートの独立ビューア（`simulator/report_ui/serve.py:12` の既定ポート 8770）。
  チャート（ライブ / リプレイ）で見つけた仮説を、同一画面のまま検証へ渡す経路が存在しない。
  本機能はこの 3 者を公開 8000 の 1 ページへ統合する。

### 2.2 開発目的・背景

- **背景**：
  - 統合 UI は既にルータ方式で 2 モードを収容済み（`unified_ui/router.py:52-55` の振り分け表・
    `.doc/LIVE_REPLAY_UNIFICATION_BASIC_DESIGN.md`）。第 3 モードの追加はこの機構の延長線上にある。
  - 口座規約（証拠金・ロスカット）の参照実装が ISSUE-369 で確定した
    （`simulator/usecase/account_engine.py:310-335` の `official_required_margin` /
    `official_losscut_price`）。サイジング規則を推測なしに UI へ結線する前提が揃った。
  - ISSUE-370（計算機のチャート統合）は「ISSUE-369 のエンジンが式の権威となること」を
    着手条件として保留されていた（ISSUE.md:6921・6947-6949）。本設計はその着手条件を満たす。
- **達成したい目標**：
  - G-1：チャートで観測した水準・指標構成から、サイジング規則と戦略規則を指定してバックテストを
    投入し、結果を同一ページで閲覧する経路を成立させる。
  - G-2：バックテスト計算をライブ core の request path に載せない（ISSUE-362 / ISSUE-364 の実測に整合）。
  - G-3：結果表示は `report_ui` の完全再現とする（パリティ検定で判定）。
  - G-4：既存モジュールは追加（OCP）で拡張する。2 値ハードコード箇所の 3 値化は
    「拡張点の欠如＝OCP 違反の是正」として表駆動化する。

### 2.3 適用範囲・制約条件

#### 2.3.1 機能要件サマリー（要件 ID 一覧）

| 要件 ID | 区分 | 概要 | 根拠 |
|---|---|---|---|
| FR-01 | モード機構 | 第 3 モード「シミュレーション」の追加（モード集合の 2 値 → 3 値化・表駆動） | 依頼者裁定 6 |
| FR-02 | モード機構 | ツールバーからのモード切替（ライブ / リプレイ / シミュレーション） | 依頼者裁定 6 |
| FR-03 | 資金管理項目 | 口座状態（残高・維持率）に依存する発注量の決定規則を指定できる | 依頼者裁定 3 |
| FR-04 | 資金管理項目 | 発注量の決定式は `account_engine` の権威式に一致する | 依頼者裁定 2・3 |
| FR-05 | 戦略項目 | インジケーター駆動の entry 条件を指定できる | 依頼者裁定 3 |
| FR-06 | 戦略項目 | インジケーター駆動の SL / TP 条件を指定できる | 依頼者裁定 3 |
| FR-07 | 戦略項目 | SL / TP の途中変更（トレーリング）を指定できる | 依頼者裁定 3（新規機能） |
| FR-08 | 戦略項目 | 部分決済（増減玉のうち減）を指定できる | 依頼者裁定 3（新規機能） |
| FR-09 | 指標供給 | ライブ側 compute の指標を因果順守で戦略へ供給できる（式の再実装をしない） | 依頼者裁定 4 |
| FR-10 | 実行系 | UI からバックテストジョブを投入できる | 依頼者裁定 5 |
| FR-11 | 実行系 | ジョブ状態（受付 / 実行中 / 完了 / 失敗）をポーリングで取得できる | 依頼者裁定 5 |
| FR-12 | 実行系 | ジョブを取り消せる | 実務的推奨（仮説）・§9.3 TBD-08 |
| FR-13 | 結果表示 | 売買マーカー hover で取引期間を表示する（区間外ローソク減光 α=0.15） | 依頼者裁定 7 |
| FR-14 | 結果表示 | 取引履歴テーブルと売買マーカーの双方向連動 | 依頼者裁定 7 |
| FR-15 | 結果表示 | 多窓同期（価格 / Balance / Drawdown の論理レンジ・クロスヘア同期） | 依頼者裁定 7 |
| FR-16 | 結果表示 | ヒートマップ（曜日 × 時間帯） | 依頼者裁定 7 |
| FR-17 | 結果表示 | 比較・判定（IS / OOS 劣化比較・判定バナー） | 依頼者裁定 7 |
| FR-18 | 結果表示 | 接点マーカー（コンタクトスキャン）の表示・トグル | 依頼者裁定 7 |
| FR-19 | 結果表示 | IS / OOS の区間切替 | 依頼者裁定 7 |
| FR-20 | 結果表示 | 用語集 | 依頼者裁定 7 |

FR-13〜FR-20 の移植元は `simulator/report_ui/web/`（実在確認済・§10.3）。

#### 2.3.2 非機能要件サマリー（数値目標を含む）

| 要件 ID | 区分 | 数値目標 | 根拠 |
|---|---|---|---|
| NFR-01 | 負荷分離 | バックテスト計算がライブ core（127.0.0.1:8001）の request path を占有する時間は 0 秒（同一プロセスで実行しない） | ISSUE-362（ISSUE.md:6698・OPEN・GIL 律速）／ISSUE-364（同 :6782・OPEN・単一ワーカー無制限待機） |
| NFR-02 | 障害分離 | sim コア停止時、ライブ・リプレイの応答は不変。ルータは `/sim/*` のみ 502 を返す | `.doc/LIVE_REPLAY_UNIFICATION_BASIC_DESIGN.md` §1（502 隔離の前例） |
| NFR-03 | ジョブ投入応答 | ジョブ投入 API の応答時間 1 秒以内（受理して job_id を返すのみ・計算を待たない） | 実務的推奨（仮説）。§9.3 TBD-09 で実測確定 |
| NFR-04 | ジョブ状態取得 | 状態取得 API の応答時間 100ms 以内。ポーリング間隔 1 秒 | 実務的推奨（仮説）。§9.3 TBD-09 で実測確定 |
| NFR-05 | 表示パリティ | `report_ui` に対するパリティ点の一致率 100%（不一致 0 件） | 依頼者裁定 7・`.doc/REPORT_UI_PARITY_DESIGN.md` |
| NFR-06 | 既存無改変 | 既存モジュールの改変は §4.5 に列挙した 9 箇所（3 値化）に限定する。それ以外の既存行変更 0 件 | 依頼者制約（SOLID・OCP） |
| NFR-07 | 依存バージョン固定 | 統合ページの lightweight-charts は v5.2.0 固定。`report_ui` 側の v4.1.3 は据え置き（同居させない） | 依頼者制約・実測（§3.5.1） |
| NFR-08 | 起動経路 | 起動は `unified_ui/serve.sh` 経由のみ。生 python 起動・臨時ポート 0 件 | プロジェクト規約（`unified_ui/serve.sh:51-70`） |
| NFR-09 | 検証経路 | 各 Phase の通過条件にブラウザ実 UI・実 HTTP 経路での確認を含める | プロジェクト規約 |

#### 2.3.3 制約条件

- **C-1（設計原則）SOLID 厳守**：設計判断を 5 原則へ写像して示す（§10.4）。既存モジュールは追加で拡張し、
  無改変を優先する。2 値ハードコードの 3 値化は OCP 違反の是正として表駆動化する。
- **C-2（禁止）破壊的変更禁止**：データの不可逆削除・認証無効化・共有モジュールの破壊的変更を行わない。
- **C-3（固定）技術スタックのバージョン無断変更禁止**：lightweight-charts は統合側 v5.2.0
  （`indigators/indicator_ui/web/vendor/lightweight-charts.js:3` に版表記を実測）、`report_ui` 側 v4.1.3
  （`simulator/report_ui/web/vendor/lightweight-charts.standalone.js:3` に版表記を実測）。両者を同一ページへ
  同居させない。
- **C-4（固定）ポート**：公開 8000・ライブ core 8001・リプレイ core 8281。sim コアの内部ポートは
  新規採番を要する（§6.5・§9.3 TBD-03）。
- **C-5（単一ソース）共有 JS**：実体は `indigators/indicator_ui/web`。共有は相対 symlink のみ（絶対 symlink 禁止）。
  新規 web スイートは `tools/web_suites.txt`（実在確認済・4 件登録）へ登録する。
- **C-6（無改変）MT5 突合系**：`simulator/usecase/run_backtest.py` と `simulator/domain/account.py` は
  MT5 突合の正であり無改変で据え置く（依頼者裁定 2）。
- **C-7（単一ソース）式の再実装禁止**：ライブ指標の式を simulator 側で再実装しない（依頼者裁定 4）。
  口座規約の式は `account_engine` を唯一の権威とする（依頼者裁定 2）。
- **C-8（用語）**：「動的」という語を使用しない。丸数字を使用しない（番号は「1.」表記）。
  同一概念に複数の呼び名を作らない。
- **C-9（進め方）**：応急処置を提示しない。未検証事項は「未検証」と明示し事実と分離する。

#### 2.3.4 設計上の課題と技術的リスク（詳細は §9）

- **課題 1（最重要・依頼者裁定を要する）**：`StrategyPort` の唯一の消費者は
  `simulator/usecase/run_backtest.py` の `RunBacktestInteractor` である（grep 実測・§3.5.3）。
  したがって「`StrategyPort` を境界として FR-07 / FR-08 を追加する」と「C-6 run_backtest.py 無改変」は
  同時に成立しない。§3.5.3 で 3 案を比較し、採否を §9.3 TBD-01 として裁定に委ねる。
- **課題 2（上流前提の訂正）**：「v5.2.0 にマーカー hover 検出が存在するかは未検証」という前提は
  実測により反証された（§3.5.2）。真のリスクは hover 検出の有無ではなく、v4 → v5 の系列生成 API の
  破壊的差分にある（§3.5.1）。
- **課題 3（依頼者確認を要する）**：サイジング規則の権威が二重になる戦略が 1 件ある。
  `weekly_vol_band` は `N = f_risk · Capital / (O − S)` をドメイン層で既に実装している
  （`simulator/domain/volatility_band.py:6, 50`）。上流入力「全 6 戦略が固定値」はこの 1 件で成立しない。
  §9.3 TBD-13 で扱いを確認する。
- **課題 4**：ライブ指標を全バー分供給する際の計算量。因果順守の逐次呼出は 1 呼出あたり実測 6.6ms
  （`simulator/replay_ui/usecase/causal_compute.py:30-36` の記載値）。1 万バーで約 66 秒となる。
  一括計算で代替できるかは指標ごとの因果性に依存する（§3.5.4・§9.3 TBD-05）。

---

## 3. システムアーキテクチャ

> 出力元：S-1 採用パターン選定 ／ S-2 アーキテクチャ設計

### 3.1 全体構成図

```
[ブラウザ] ──8000──> [unified_ui/router.py（既存・振り分け表を 3 値へ表駆動化）]
     ├─ 静的 /                → unified_ui/web（統合エントリ・モードトグル 3 値）
     ├─ /live/*      （静的+API）→ 127.0.0.1:8001   ライブ core（indicator_ui・無改変）
     ├─ /replay/*    （静的+API）→ 127.0.0.1:8281   リプレイ core（serve_replay・無改変）
     └─ /sim/*       （静的+API）→ 127.0.0.1:[TBD-03] ★ sim コア（新規）
                                        ※内部ポートは loopback 限定・外部非公開

★ sim コア（新規・simulator/sim_ui/ = replay_ui と同型構成）
   framework  : static_file_server（shared_js_root フォールバック）＋ HTTP API
   main       : composition_root（DI 一括結線）
   adapter    : ジョブ台帳ゲートウェイ／指標供給ブリッジ／結果ペイロード読出
   usecase    : ジョブ受付 UC・ジョブ状態照会 UC・結果取得 UC
   domain     : ジョブ状態（受付/実行中/完了/失敗/取消）の遷移規則
        │
        │ 子プロセス起動（request path を占有しない＝NFR-01）
        ▼
★ バックテスト実行 CLI（新規・子プロセス）
        │   1. 指標供給: ライブ compute を因果順守で IndicatorPort へ（§3.5.4）
        │   2. 発注量決定: SizingPort ← account_engine の権威式（§3.5.5）
        │   3. 実行エンジン: §3.5.3 の裁定に従う（TBD-01）
        ▼
★ 結果ペイロード JSON（report_ui の JSON 契約と同形）
        │
        ▼ GET /sim/data/<job_id>/report.json
★ sim モードのフロント（unified_ui/web 配下・report_ui からの移植）
        ・純ロジック（linkage.js・balanceForwardFill 等）はそのまま流用
        ・vendor 層（系列生成・マーカー適用）のみ v4.1.3 → v5.2.0 へ移植（§3.5.1）
        ▼
[ブラウザ] 閲覧者
```

依存方向は既存 simulator のクリーンアーキテクチャを踏襲する
（framework → adapter → usecase → domain・内向き）。フロントは JSON 契約のみに依存する。

### 3.2 アーキテクチャパターン選択理由（プロセス構成）

- **採用：ルータ配下のプロセス分離（既存 2 モードのルータ方式へ第 3 上流を追加）**

| パターン | 概要 | NFR-01 負荷分離 | NFR-02 障害分離 | 既存改変量 | 採否 |
|---|---|---|---|---|---|
| 案 1：ルータ配下の別プロセス core（採用） | `/sim/*` を新設 core へプロキシ。計算はさらに子プロセスへ | 適合（ライブ core と別プロセス） | 適合（sim のみ 502） | 小（振り分け表の 1 行追加＋3 値化） | **採用** |
| 案 2：ライブ core に sim API を相乗り | indicator_ui のサーバへエンドポイント追加 | **不適合**（ISSUE-362 GIL 律速・ISSUE-364 単一ワーカー詰まりへ直撃） | 不適合（sim の重処理でライブが停止） | 中 | 棄却 |
| 案 3：リプレイ core に相乗り | serve_replay へエンドポイント追加 | 部分適合（ライブとは別プロセス） | 不適合（リプレイが sim の重処理で停止） | 中 | 棄却 |

- **採用根拠**（プロジェクト規約＋実測）：
  - 実測 1（ISSUE.md:6698・ISSUE-362・OPEN）：サーバ計算量が律速でありクライアント側の一括化だけでは
    1 秒保証に到達しない。重い計算をライブ core の request path へ追加する案 2 は、この実測と正面から矛盾する。
  - 実測 2（ISSUE.md:6782-6788・ISSUE-364・OPEN）：`/market_profile` は専用スレッド 1 本へ投函され
    `done.wait()` が無制限に待つ。一度冷えると以後すべてタイムアウトする。案 2 / 案 3 は同一の
    詰まり方を sim へ再現させる。
  - 前例：ルータの 502 隔離は `.doc/LIVE_REPLAY_UNIFICATION_BASIC_DESIGN.md` §1 で確立済み。
  - 棄却理由（案 2 / 案 3）：負荷分離・障害分離のいずれも満たさない。既存の実測済み障害を新モードへ
    複製することになる。

- **さらに sim コア内でも計算を子プロセスへ出す根拠**：sim コア自身も HTTP サーバであり、
  リクエスト処理スレッド上でバックテストを実行すると sim コア内で ISSUE-364 と同型の詰まりが再発する。
  ジョブを子プロセスへ出し、sim コアは台帳の読み書きのみを行う（依頼者裁定 5 に整合）。

### 3.3 アーキテクチャパターン選択理由（sim コア内部構造）

- **採用：クリーンアーキテクチャ（`simulator/replay_ui/` と同型）**

| パターン | 概要 | 採否 |
|---|---|---|
| 案 A：`replay_ui` 同型のクリーンアーキ（採用） | framework / main（composition_root）／adapter／usecase／domain の 5 層。static_file_server の shared_js_root フォールバックを踏襲 | **採用** |
| 案 B：単一モジュールの薄い HTTP サーバ | ジョブ起動と JSON 返却のみの最小実装 | 棄却 |

- **採用根拠**（プロジェクト規約 `BACKTEST_CLEAN_ARCH.md`／公式設計原則 Martin 2017『Clean Architecture』）：
  - 実測：`simulator/replay_ui/` は framework（`static_file_server.py`・`serve_replay.py`）／
    main（`composition_root.py:30` の `build_replay_app`）／adapter（10 モジュール）／usecase（8 モジュール）／
    domain（3 モジュール）の構成で実在し、同型を採ることで構造の学習コストと審査コストが増えない。
  - 実測：`static_file_server.py:90-103` の `resolve()` は web_dir を優先し miss で shared_js_root へ
    同一 rel でフォールバックする。共有 JS が sim 配下に物理不在でも配信できる（C-5 の単一ソース維持に必須）。
  - 棄却理由（案 B）：ジョブ状態の遷移規則（受付 → 実行中 → 完了 / 失敗 / 取消）はドメイン知識であり、
    HTTP ハンドラへ埋めると SRP 違反となる。またテスト境界が HTTP のみとなり、状態遷移の単体検証ができない。

### 3.4 技術スタック詳細

| 層 | 採用技術 | バージョン | 代替候補 | 採用根拠 |
|---|---|---|---|---|
| ルータ | 既存 `unified_ui/router.py`（Python 標準 `http.server`） | 既存準拠 | nginx 等の外部プロキシ導入 | C-3。既存の振り分け表（`router.py:52-55`）へ 1 モード追加するのが最小差分。外部プロキシは新規依存かつ serve.sh 起動規約（NFR-08）を壊す |
| sim コア | Python（既存 simulator と同一）・標準ライブラリ HTTP | 既存準拠 | Node.js / FastAPI 等 | C-3。`replay_ui` が同一構成で稼働中（`serve_replay.py` 実在） |
| ジョブ実行 | OS 子プロセス（バックテスト CLI を起動） | — | スレッドプール／外部ジョブキュー（Celery 等） | NFR-01。スレッドは GIL を共有し ISSUE-362 の律速を回避できない。外部ジョブキューは新規依存（C-3 違反） |
| ジョブ台帳 | ファイルシステム上の JSON（job ごとに 1 ディレクトリ） | stdlib json | SQLite / Redis | 新規依存ゼロ。単一ホスト・単一 sim コアの前提で足りる。§9.3 TBD-06 で同時実行数の上限方針を確定 |
| 結果契約 | JSON（`report_ui` の JSON 契約と同形） | stdlib json | Protobuf / MessagePack | 既存 `report_ui` フロントの純ロジックをそのまま流用するには同形が必須（NFR-05） |
| 価格チャート | lightweight-charts（統合ページ同梱 vendor） | **v5.2.0**（実測） | report_ui の v4.1.3 を同居 | C-3・NFR-07。1 ページに 2 版を同居させるとグローバル `LightweightCharts` が衝突する |
| 補助グラフ | Chart.js（`report_ui/web/vendor/chart.umd.js` と同一 vendor） | 既存準拠 | D3.js | 移植元と同一にすることでグラフ描画のパリティ差分を vendor 起因から排除する |
| フロント基盤 | ES Modules（バンドル不要・統合 UI と同方針） | 既存準拠 | React / Vue | C-3。統合 UI は素の ESM で構成されている（`unified_ui/web/js/*.js` 実在） |

### 3.5 主要設計判断

#### 3.5.1 表示移植：vendor 層の v4.1.3 → v5.2.0 移植（実測に基づく真の作業量）

- **実測（証拠先行）**：

  | # | 実測項目 | コマンド／参照 | 結果 |
  |---|---|---|---|
  | 1 | 統合側 vendor の版 | `indigators/indicator_ui/web/vendor/lightweight-charts.js:3` | `TradingView Lightweight Charts™ v5.2.0` |
  | 2 | report_ui 側 vendor の版 | `simulator/report_ui/web/vendor/lightweight-charts.standalone.js:3` | `TradingView Lightweight Charts™ v4.1.3` |
  | 3 | v5.2.0 に v4 系列生成 API が在るか | v5.2.0 バンドルを `addCandlestickSeries\|addAreaSeries\|addBaselineSeries` で grep | **0 件（不在）** |
  | 4 | report_ui が前提とする API | `simulator/report_ui/web/js/chart.js:3-5` | `addCandlestickSeries / addAreaSeries / addBaselineSeries` を明示的に前提 |
  | 5 | v5 での系列生成の実装 | `indigators/indicator_ui/web/js/adapter/front/chart_renderer.js:4,23-24,128` | `addSeries(CandlestickSeries, ...)` / `addSeries(LineSeries, ...)` |
  | 6 | v4 のマーカー適用 | `simulator/report_ui/web/js/chart.js:307,319` | `series.setMarkers(markers)` |
  | 7 | v5 のマーカー適用 | `indigators/indicator_ui/web/js/adapter/front/trade_markers_renderer.js:339-344` | 初回 `createSeriesMarkers(series, markers)` でハンドル生成、以降ハンドルへ `setMarkers` |

- **判定**：移植の主作業は「hover が動くか」ではなく **系列生成 API とマーカー適用 API の総取替**である。
  実測 3 により v4 の系列生成 API は v5.2.0 に存在しないため、`chart.js` の vendor 依存部は
  そのままでは 1 行も動作しない。これは選択ではなく必須作業である。
- **不変とする範囲**：`report_ui/web/js/linkage.js`（全 42 行・DOM / vendor 非依存の純状態機械であることを
  Read で確認）、および `chart.js` の純ロジック（`dedupeCurve` / `balanceForwardFill` /
  `byTimeResolve` / `contactToMarker` / `contactsInRange` / `contactsToMarkers`＝同ファイル :42-102 が
  vendor 非依存であることを Read で確認）はそのまま流用する。
- **多窓構成の代替案比較**：

  | 案 | 概要 | パリティ（NFR-05） | 実装量 | 採否 |
  |---|---|---|---|---|
  | 案 a：3 チャートインスタンス維持・API のみ置換（採用） | 価格 / Balance / Drawdown を別インスタンスとし、論理レンジ相互同期を移植元どおり保つ | 移植元と同一構造＝差分が API 呼出に限定され、パリティ不一致の原因を切り分けられる | 中 | **採用** |
  | 案 b：v5 の pane 機能へ寄せる | 1 チャートの複数 pane（`addPane` / `panes`・実測 `chart_renderer.js:4,644`）へ統合 | 同期の実装主体が vendor 側へ移り、移植元の同期挙動と一致する保証がない | 中 | 棄却 |

  - 採用根拠（プロジェクト規約 NFR-05）：パリティ検定の合格条件を変えないため、構造は移植元と同一に保ち、
    差分を vendor API 呼出のみに閉じる。案 b は表示構造自体を変えるため、不一致が出た際に
    「移植ミス」と「pane 化による仕様差」を分離できない。案 b は本機能の完成後に別途検討する。

#### 3.5.2 上流前提の訂正：v5.2.0 のマーカー hover 検出は実測済み

- **上流入力**：「v5.2.0 にマーカー hover 検出（v4 の `param.hoveredObjectId` 相当）が存在するかは未検証。
  設計書にリスクと検証手順を記載し、NG の場合の代替設計（座標→マーカー索引の自前ヒットテスト）も記す」。
- **実証（証拠先行・判定より先に提示）**：

  | # | 実証手段 | 参照箇所 | 出力 |
  |---|---|---|---|
  | 1 | 実コード Read | `indigators/indicator_ui/web/js/adapter/front/trade_markers_renderer.js:96-103` | `_onCrosshair(param)` が `param.hoveredObjectId` を**唯一の発火信号**として本番稼働。統合 UI（v5.2.0 vendor）で配信中 |
  | 2 | ISSUE 記録 Read | `ISSUE.md:387`（ISSUE-025・ステータス RESOLVED・検出日 2026-06-21） | 「**lwc v5.2.0** は marker のテキスト範囲もヒット領域に内包するため、ラベル上 hover でも `hoveredObjectId` がセットされ v8 の減光が発火する。**ブラウザ実測**：ラベル帯（y≈45）hover でマーカー列輝度 32.43→19.34（−40.4%）」 |
  | 3 | 設計記録 Read | `.doc/CHART_TRADE_MARKERS_DETAILED_DESIGN.md:364-366`（§13.1-1） | 「`param.hoveredObjectId` は正しく発火する。グリフ直上で `t478:exit` を検出した」＝**実ブラウザ計測** |
  | 4 | vendor バンドル grep | v5.2.0 バンドルを `hoveredObjectId` で grep | 検出（シンボル在中） |

- **判定：上流前提を棄却する（証拠により反証済み）。** v5.2.0 における `param.hoveredObjectId` の
  マーカー hover 発火は、ブラウザ実測で 2 度確認され、現在も本番稼働している。したがって
  「未検証リスク」として設計書に残すことは、実証済みの事実を未確定として扱う誤りとなる。
- **代替設計（自前ヒットテスト）も棄却する**：`.doc/CHART_TRADE_MARKERS_DETAILED_DESIGN.md:367-371`（§13.1-2）に、
  座標近接方式（v7）が実ブラウザ計測で破綻した記録がある。「実グリフ画素は約定価格画素から約 130px
  離れている（実測 dy ≈ −130）。上流（lwc）が実グリフ画素を公開しない以上、近接ピクセル方式は
  原理的に成立しない」。同方式を代替案として提示することは、破綻が実証済みの方式を選択肢に並べる行為であり、
  C-9（応急処置の提示禁止）に該当する。
- **残る真の未検証事項（§9.3 TBD-04 へ）**：`report_ui` は売買マーカーと接点マーカーを**別系列**へ分離している。
  理由は `chart.js:13-15` に「v4.1.3 はシリーズ単位で上書きになるため」と記録されている。v5 の
  `createSeriesMarkers` はハンドル単位であり、同一系列へ複数ハンドルを付与できるかは未検証である。
  これは hover 検出とは別の論点であり、Phase の通過条件として実測する。
- **マーカー id 規約の差異（設計事項）**：移植元 `report_ui` は `"e"+id` / `"x"+id`（`chart.js:295-304`）、
  ライブ側は `"t{i}:entry"` / `"t{i}:exit"`（`trade_markers_renderer.js:91`）。sim モードは移植元の
  id 規約を維持する（パリティ NFR-05 のため）。ライブ側の規約へ寄せない。

#### 3.5.3 実行エンジンの境界（最重要・依頼者裁定を要する）

- **論点**：FR-07（トレーリング）・FR-08（部分決済）・FR-03/04（SizingPort 結線）を
  `StrategyPort` 境界で実現する裁定と、C-6（`run_backtest.py` 無改変）が同時に成立するか。

- **実証（証拠先行）**：

  | # | 実証手段 | 結果 |
  |---|---|---|
  | 1 | `simulator/`（tests 除く）を `StrategyPort\|on_new_bar\|on_position_check` で grep | 消費者は `simulator/usecase/run_backtest.py` **のみ**。他のエンジンは存在しない。実装側は `adapter/strategy/` の 6 戦略 |
  | 2 | `run_backtest.py:326-331 / 598-603 / 662-664` を Read | `_OpenTrade(position=..., sl=order.sl, tp=order.tp, entry_time=...)` — SL / TP は建玉時に確定保持される |
  | 3 | 同ファイル全体を `trailing\|partial_close\|modify_sl` で grep | 建玉後に SL / TP を再設定する経路 0 件 |
  | 4 | `simulator/domain/order.py:60-63` を Read | `kind` の許可集合は `{market, buy_limit, sell_limit, buy_stop, sell_stop}`。`trailing_stop` は明示的に `InvalidPriceError`（`simulator/tests/unit/test_order.py:93-96` が否定テストとして固定） |
  | 5 | `simulator/adapter/presenter/trade_markers.py:77` を Read | 「部分決済は当エンジンに無い」と記載 |
  | 6 | `simulator/domain/account.py:40-92` を Read | MT5 規約（`leverage` / `contract_size`・`margin_level = equity / margin * 100`）。`account_engine.py:85-105` の OANDA 規約（`margin_rate` / 維持率 100% 以下でロスカット）とは別体系 |

- **判定**：実証 1 により、`StrategyPort` を消費するエンジンは `run_backtest.py` ただ 1 つである。
  よって FR-07 / FR-08 を `StrategyPort` 境界で実現するには、当該エンジンが建玉の SL / TP を毎バー
  再評価し、部分決済を実行する経路を持つ必要がある。実証 2〜5 によりその経路は存在しない。
  **したがって「StrategyPort 境界で FR-07 / FR-08 を追加する」と「run_backtest.py 無改変」は同時に成立しない。**

- **代替案比較（採否は依頼者裁定を要する・§9.3 TBD-01）**：

  | 評価軸 | 案 A：OANDA シミュレーション UC を新設 | 案 B：既存 Interactor へ拡張点を追加 | 案 C：エンジン骨格を抽出し口座規約を差替可能に |
  |---|---|---|---|
  | 概要 | `StrategyPort` / `IndicatorPort` / `TickModelPort` を再利用する新 UC を追加。口座規約は `account_engine` を使う。FR-07 / FR-08 を新 UC が実装 | `run_backtest.py` に任意 DI（発注量決定・建玉変更の Port）を追加。既定 None で既存経路は byte 等価 | 共通骨格を抽出し、MT5 規約と OANDA 規約を差替可能な境界の実装として並立 |
  | C-6（run_backtest.py 無改変） | **適合**（1 行も触らない） | 不適合（既存行に分岐を追加） | 不適合（大幅改変） |
  | MT5 突合の回帰リスク | なし（既存経路に触れないため構造的にゼロ） | 中（既定 None で byte 等価を golden で保証する必要あり） | 高 |
  | 依頼者裁定 2 の文言適合 | 「別ユースケースとして並立させる構成にはしない」との文言と衝突しうる（文言解釈に依存・下記参照） | 「無改変で据え置き」との文言と衝突 | 両文言と衝突 |
  | ロジック重複 | 中（バー走査・約定判定の骨格が 2 箇所に存在） | なし | なし |
  | 実装量 | 大 | 中 | 最大 |
  | OCP 適合 | 適合（既存を変更せず追加で拡張） | 部分適合（拡張点を開けるための既存変更が 1 度必要） | 不適合（既存の全面改変） |

- **依頼者裁定 2 の文言に 2 通りの解釈が成立する（設計側で決定しない）**：
  「口座規約は OANDA に一本化する。MT5 突合系は無改変で据え置き、別ユースケースとして並立させる構成にはしない」。
  1. 解釈 1：「並立させない」の目的語は**口座規約**である。すなわち OANDA 規約の実装を
     `account_engine` 以外に作らないという意味であり、実行 UC が 2 つ在ることは許容される（案 A が成立）。
  2. 解釈 2：「並立させない」の目的語は**ユースケース**である。すなわち実行 UC を 2 つにしない
     という意味であり、既存 UC を拡張する必要がある（案 B / 案 C）。ただしこの解釈は同一文中の
     「無改変で据え置き」と両立しない。
  実証手段（grep / Read）で解決できるのは事実関係のみであり、文言の意図は実証できない。
  C-9（証拠不在の前提を所与にしない）に従い、**設計側で一方的に確定させず §9.3 TBD-01 として裁定を求める**。

- **裁定が下るまで確定できる範囲（本書で確定させる）**：FR-03 / FR-04（SizingPort）の**結線方式**は
  §3.5.5 の Decorator により、**戦略・エンジンの双方を無改変のまま**成立する。したがって
  TBD-01 の裁定を待たずに Phase 1〜2 へ着手できる。ただし Phase 2 の完了には TBD-13
  （既存サイジングを持つ戦略の扱い）と TBD-14（推定建値の乖離許容）の確認を要する。
  FR-07 / FR-08 のみが TBD-01 に依存する。

#### 3.5.4 指標供給：ライブ compute の因果順守供給

- **実証**：
  - `simulator/replay_ui/usecase/causal_compute.py:1-11` の手順は
    「1. `load_source(ref, tf)` → 2. `RevealClock.truncate(bars, untilTime)` → 3. `tail(limit)` →
    5. `latest` / `full` 計算」であり、truncate により「再生のその時点まで＝因果不変」を保証する。
  - `simulator/replay_ui/adapter/_indicator_ui_bridge.py:1-15` は indicator_ui の実装を
    **読むだけ**で再利用する境界であり、ISSUE-136 の ISP 分割により `load_dataset` /
    `load_compute` / `load_mp_handlers` の粒度別アクセサを提供する。既存 indicator_ui は無改変。
  - 計算量の実測記載：`causal_compute.py:30-36` に「load_source 242ms / latest 6.6ms」。

- **代替案比較**：

  | 案 | 概要 | 計算量（1 万バー） | 因果順守の保証 | 採否 |
  |---|---|---|---|---|
  | 案 i：バーごとに truncate して逐次計算 | 各バー i で `until_time = bar_i.time` として計算 | 約 66 秒（6.6ms × 10,000。load_source は 1 回） | 構造的に保証（未来バーが窓に入らない） | 条件付き採用（照合基準として使う） |
  | 案 ii：全期間を 1 回 full 計算し系列を IndicatorPort へ渡す | 全バーを一度に計算し、バー i の値を配列から引く | 約 0.25 秒（load_source 1 回＋full 1 回） | **指標が因果的である場合に限り**一致する。非因果指標（中心化・再描画を伴うもの）では一致しない | 採用（ただし指標ごとに検定を通過したものに限る） |

- **採用方針（公式設計原則 DIP／実務的推奨・仮説）**：案 ii を既定とし、**案 i を照合基準（golden）とする
  検定を Phase の通過条件に置く**。指標ごとに、案 ii の系列と案 i の系列が全バーで一致することを実測し、
  一致した指標のみを sim モードで選択可能にする。不一致の指標は選択不可として明示する
  （無音で誤った値を使わない）。既定を案 i にしない理由は 66 秒が NFR-03 のジョブ体験を損なうためだが、
  正しさの基準は案 i に置く。
- **未検証事項**：どの指標が案 ii と案 i で一致するかは未検証である（§9.3 TBD-05）。
  指標ごとの因果性を推測で断定しない。

#### 3.5.5 SizingPort：戦略・エンジン双方を無改変とする Decorator 方式

- **実証**：

  | # | 実証手段 | 結果 |
  |---|---|---|
  | 1 | `simulator/adapter/strategy/` を `lot_size\|volume=` で grep | 6 戦略が発注量を自分で確定。うち **5 戦略が固定値**（`tc24051901.py:76` / `pro_fit_band.py:127` / `ma_slope.py:108` / `ma_slope_pending.py:119` / `stop_entry_probe.py:80` が `volume=cfg["lot_size"]`） |
  | 1b | `weekly_vol_band.py:57-72` と `simulator/domain/volatility_band.py:6, 40-50` を Read | **1 戦略は固定値ではない**。`weekly_vol_band` は `volume=N` で、`N = f_risk · Capital / (O − S)`（資金比リスクに基づくサイジング）を**ドメイン層で既に実装している**。上流入力「全 6 戦略が `volume=cfg["lot_size"]` 固定」はこの 1 件について成立しない（§10.5 U-7 の訂正） |
  | 2 | `run_backtest.py:89-106` を Read | `RunBacktestInteractor.__init__(*, strategy, indicators, tick_model, session_calendar=None)` — `strategy` は DI で受ける |
  | 3 | `run_backtest.py:286-290, 305` を Read | `orders = self._strategy.on_new_bar(bar_index, self._indicators, account)` の戻り値をエンジンが消費する。`account`（口座状態）は戦略へ渡されている |
  | 4 | `run_backtest.py:818-824` を Read | `rearm = self._strategy.on_tick(bar_index, q_bid, q_ask, account)` — ティック経路も同型 |
  | 5 | `simulator/domain/order.py:45-52` を Read | `Order` は `@dataclass(frozen=True)`。属性は side / kind / volume / price / sl / tp |
  | 6 | `run_backtest.py:300-304` を Read | 実際の建値は `derive_quotes(bar, entry_price_basis=config.entry_price_basis, point_size=spec.point_size)` がエンジン内部で決める。**戦略にも Decorator にも渡らない** |
  | 7 | `ma_slope.py:62, 69-71` と `weekly_vol_band.py:61` を Read | `on_new_bar` の引数に価格・バーは無い。戦略は価格を指標レジストリ経由で得る（`indicators.get("ema").iloc[...]` / `indicators_open_at(indicators, 0)`） |

- **判定**：実証 2〜4 により、`StrategyPort` の実装を**別の `StrategyPort` 実装で包んで DI する**ことで、
  戦略が返した発注の `volume` をエンジンへ渡る前に差し替えられる。包む側は `on_new_bar` / `on_tick` の
  戻り値である発注列を受け取り、`account`（実証 3・4 で戦略へ渡されている口座状態）と
  `SizingPort` の決定式から発注量を再計算し、`Order` を再構築して返す（実証 5 により `Order` は
  不変オブジェクトのため、属性の書換ではなく同値の再構築とする）。
- **成立する無改変の範囲**：
  - 戦略 6 本：無改変（`volume=cfg["lot_size"]` のまま）。
  - `run_backtest.py`：無改変（DI される実装が変わるだけ）。
  - `simulator/usecase/ports.py`：`SizingPort` を**追加**する（既存 Port の変更なし＝ISP）。
- **代替案比較**：

  | 案 | 概要 | 戦略の改変 | エンジンの改変 | 採否 |
  |---|---|---|---|---|
  | 案 x：`StrategyPort` の Decorator で発注量を差し替える（採用） | 既存戦略を包む `StrategyPort` 実装を新設し、`SizingPort` を注入 | なし | なし | **採用** |
  | 案 y：各戦略に `SizingPort` を注入する | 6 戦略の `on_new_bar` を書き換え、`volume` を Port から取得 | 6 ファイル | なし | 棄却 |
  | 案 z：エンジンが発注列を受けた直後に量を差し替える | `run_backtest.py:305` の直前に差替処理を追加 | なし | あり（C-6 違反） | 棄却 |

  - 採用根拠（公式設計原則：Decorator パターン／OCP・Martin 2017）：案 x は既存の 2 種類の資産
    （戦略・エンジン）のいずれも変更せず、追加のみで発注量の決定規則を差し替える。
    案 y は 6 ファイルの改変が必要で、MT5 突合に使われている戦略（`stop_entry_probe` は
    `simulator/tests/confirmation/2026-04_stop-probe/ea.mq5` が原典）へ手を入れるため突合の回帰リスクを負う。
    案 z は C-6 に違反する。
- **成立しない範囲（実証 1b・6・7 から導かれる制約・上流入力の訂正）**：
  1. **サイジング規則の権威が二重になる戦略が 1 件ある**。`weekly_vol_band` は
     `N = f_risk · Capital / (O − S)`（`domain/volatility_band.py:6, 50`）という資金比リスクの
     サイジングを既に持つ。この戦略へ Decorator を適用すると、ドメインの規則と `SizingPort` の規則が
     二重に適用され、どちらが権威か決まらない。**Decorator の適用は戦略ごとの明示指定とし、
     全戦略へ一律に適用しない**。既存のサイジングを持つ戦略を sim モードの対象に含めるか、
     含める場合にどちらを権威とするかは §9.3 TBD-13。
  2. **決定時点の価格は実際の建値と一致しない**。`official_required_margin` は建値 P を入力に要するが、
     実証 6 により実際の建値はエンジン内部の `derive_quotes` が決め、Decorator へは渡らない。
     実証 7 により Decorator が得られる価格は指標レジストリ経由の値（バーの始値等）に限られる。
     したがって発注量は「決定時点の推定建値」に基づく。**式は権威式に一致するが、入力の建値は
     推定値である**。この差の許容範囲は §9.3 TBD-14。ティック経路（`on_tick`）は bid / ask が
     引数で渡るため、この差は生じない。
- **発注量の決定式の権威**：`account_engine.py:310-316` の `official_required_margin`
  （`M = Σuᵢ·Pᵢ·V × mr`・公式 §3(2) 約定代金基準）および `:319-335` の `official_losscut_price`
  （long: `X = avgP·(1+mr) − E/(U·V)` / short: `X = avgP·(1−mr) + E/(U·V)`）を唯一の権威とする。
  `SizingPort` の実装はこの関数群を呼び出し、式を再実装しない（C-7）。
- **口座規約の不一致に関する明示**：MT5 突合系の口座（`domain/account.py:62-66`：
  `margin_level = equity / margin * 100`・`required_margin` は `leverage` / `contract_size` 由来）と
  OANDA 規約（`account_engine.py:89-90`：`margin_rate=0.10`・`losscut_ratio=1.00`）は別体系である。
  Decorator が参照する口座状態をどちらの体系で評価するかは、§3.5.3 の TBD-01 の裁定に連動する
  （§9.3 TBD-02）。

#### 3.5.6 モード機構の 3 値化：OCP 違反の是正としての表駆動化

- **実証（9 箇所の全数確認・依頼者提示の一覧を実コードで照合）**：

  | # | 箇所 | 実測内容 | 3 値化の方式 |
  |---|---|---|---|
  | 1 | `unified_ui/web/js/mode_ui_view.js:16` | `MODE = Object.freeze({ LIVE: 'live', REPLAY: 'replay' })` | モード定義に SIM を追加（定義表を単一ソース化） |
  | 2 | 同 :68-78 | `applyModeUi` が `um-mode-live` / `um-mode-replay` の 2 クラスを個別にトグル | モード定義表を走査して全モードのクラスをトグル（モード追加時に本体不変） |
  | 3 | `unified_ui/web/js/unified_root.js:111-126` | `toggle(next)` は**既に明示ターゲット引数を受ける**。2 値反転は `next` 省略時の既定算出のみ | 既定算出を定義表の巡回へ置換。呼出側は `toggle('sim')` で足りる |
  | 4 | `unified_ui/router.py:52-55` | `_PREFIX_TO_UPSTREAM_ATTR = (("/live","live_upstream"),("/replay","replay_upstream"))`＝**既に表駆動** | 表へ `("/sim","sim_upstream")` を 1 行追加 |
  | 5 | 同 :316-324 | `create_router_server(bind_addr, *, live_upstream, replay_upstream, web_root, ...)` | `sim_upstream` を追加（既定値ありで後方互換） |
  | 6 | 同 :364-373 | CLI 引数 `--live-upstream` / `--replay-upstream` | `--sim-upstream` を追加 |
  | 7 | `unified_ui/web/js/sw_rewrite.js:63-69, 80` | prefix 判定と付与が `/live` / `/replay` のハードコード。ただし同ファイル :41-43 に `LIVE_ONLY_SEGMENTS` という**表駆動の拡張点が既に存在**（OCP 是正の前例） | 同流儀でモード prefix の表を導入し、判定・付与を表参照へ |
  | 8 | `unified_ui/web/sw.js:25-34` | `data.mode === 'live' \|\| data.mode === 'replay'` の 2 値受理 | 許可モード集合を表から判定 |
  | 9 | `unified_ui/web/js/routed_fetch.js:67-70` | `return m === 'replay' ? 'replay' : 'live'` — **未知値を live へ倒す**。`'sim'` を渡すと live core へ誤配される | 許可集合に含まれるモードのみ通し、含まれない値のみ既定へ倒す |
  | 10 | `indigators/indicator_ui/web/js/adapter/front/app_chrome_view.js:48, 79-82` | `installChartToolbar(doc, { anchor, liveFollow, enterReplay })` — `enterReplay` の真偽 1 個でリプレイボタンの有無を制御 | ボタン定義を配列で受ける形へ拡張（既定は現状と同一＝後方互換） |
  | 11 | `unified_ui/serve.sh:60-62` | `PUBLIC_PORT=8000` / `LIVE_PORT=8001` / `REPLAY_PORT=8281` | `SIM_PORT` を追加（値は §9.3 TBD-03） |

  > 依頼者提示の一覧（9 点）と実コードは全て一致した。上表は同一ファイル内で行が離れる箇所を
  > 分離して数えたため 11 行になっている（対象箇所の集合は同一）。

- **設計方針（公式設計原則 OCP・Martin 2017）**：モード集合を**単一の定義表**として持ち、
  上記すべての分岐を表の走査へ置換する。第 4 モードを追加する際に本体コードを変更しない状態を
  到達点とする。これは新機能の追加ではなく、**拡張点の欠如という既存の設計欠陥の是正**である。
- **実測に基づく作業量の訂正**：#3（`toggle` は既に明示ターゲットを受ける）と #4（振り分け表は既に表駆動）と
  #7（表駆動の拡張点が既に存在）により、3 値化の実作業は当初想定より小さい。
  一方 #9（`routed_fetch.js:69` が未知モードを live へ無音で倒す）は、表駆動化を怠ると
  **sim モードの API がライブ core へ誤配され、エラーにならずに誤った結果を返す**危険がある。
  この箇所を最優先で是正する（Phase 1 の通過条件）。

---

## 4. 機能設計

> 出力元：S-3 機能設計

### 4.1 機能一覧・優先度

優先度は Phase 順（§8.1）と一致する。Phase 1 から実測検証可能な最小単位で刻む。

| 機能 ID | 機能名 | 概要 | Phase | 対応要件 ID |
|---|---|---|---|---|
| F-1 | モード機構の 3 値化 | モード定義表の単一ソース化と全分岐の表駆動化（§3.5.6 の 11 箇所） | 1 | FR-01, FR-02 |
| F-2 | sim コアの骨格 | `replay_ui` 同型の core 新設。ルータ `/sim/*` 上流追加・静的配信・疎通 | 1 | FR-01 |
| F-3 | ジョブ実行系 | ジョブ受付・子プロセス起動・状態遷移・状態照会・結果取得 | 2 | FR-10, FR-11, FR-12 |
| F-4 | SizingPort | 発注量決定の境界新設と Decorator による結線（戦略・エンジン無改変） | 2 | FR-03, FR-04 |
| F-5 | 指標供給 | ライブ compute の因果順守供給と因果性検定 | 3 | FR-09 |
| F-6 | 表示移植（骨格） | 多窓チャート・取引履歴テーブル・双方向連動の v5.2.0 移植 | 4 | FR-13, FR-14, FR-15 |
| F-7 | 表示移植（周辺） | ヒートマップ・比較判定・接点マーカー・IS/OOS 切替・用語集 | 5 | FR-16〜FR-20 |
| F-8 | 戦略項目の指定 | インジケーター駆動の entry / SL / TP の指定 | 6 | FR-05, FR-06 |
| F-9 | 建玉変更機能 | トレーリング・部分決済（**TBD-01 の裁定に依存**） | 7 | FR-07, FR-08 |

### 4.2 機能詳細仕様（主要機能のみ）

- **F-3 ジョブ実行系（FR-10 / FR-11 / FR-12）**
  - 入力：戦略項目の指定・資金管理項目の指定・対象期間・対象シンボル / 時間足。
  - 出力：ジョブ識別子（投入時）／ジョブ状態（照会時）／結果ペイロードの所在（完了時）。
  - 前提：sim コアは受理と台帳の読み書きのみを行い、計算を自プロセスで実行しない（NFR-01）。
  - 後条件：完了時に結果ペイロードが読み出し可能な状態で存在する。失敗時は失敗理由が状態に含まれる。
  - 例外条件：子プロセスの異常終了は「失敗」状態へ遷移させ、部分的な結果ペイロードを公開しない
    （既存 `report_ui` の fail-stop 方針を踏襲・`.doc/sim-report-ui/基本設計書.md` §7.2）。
  - 例外条件：同時実行数の上限に達した投入は受理せず、その旨を返す（上限値は §9.3 TBD-06）。

- **F-4 SizingPort（FR-03 / FR-04）**
  - 入力：口座状態（残高・保有建玉・維持率）、対象銘柄の建値、サイジング規則の指定値。
  - 出力：発注量。
  - 前提：必要証拠金・ロスカット価格は `account_engine` の権威式のみを用いる（C-7）。
  - 後条件：戦略が返した発注の量のみが差し替わり、方向・種別・価格・SL・TP は不変。
  - 例外条件：算出量が銘柄の最小取引単位・刻みを満たさない場合の扱いは §9.3 TBD-07。

- **F-6 表示移植・骨格（FR-13 / FR-14 / FR-15）**
  - 入力：結果ペイロード（`report_ui` の JSON 契約と同形）。
  - 出力：価格 / Balance / Drawdown の 3 窓・取引履歴テーブル・双方向ハイライト。
  - 前提：純状態機械（移植元 `linkage.js`）と純ロジック（移植元 `chart.js:42-102`）は無改変で流用する。
  - 前提：vendor 依存部は v5.2.0 の系列生成 API・マーカーハンドル API へ置換する（§3.5.1）。
  - 後条件：売買マーカー hover 時、当該取引の `[entry_time, exit_time]` 区間外のローソクが
    α=0.15 で減光される（移植元 `chart.js:19, 333-343` と同一の減光値・同一の区間規則）。
  - 例外条件：ペア外マーカーの減光・ホバー解除時の復元は移植元と同一挙動とする。

### 4.3 処理フロー図

```
[投入]
  ブラウザ（sim モード）
    │ 戦略項目・資金管理項目・期間を指定して投入
    ▼ POST /sim/jobs
  ルータ ──> sim コア（受付 UC）
    │ 1. 入力検証  2. ジョブ台帳へ「受付」で登録  3. 子プロセス起動  4. job_id 返却（NFR-03: 1 秒以内）
    ▼
  バックテスト CLI（子プロセス）
    │ 1. 指標供給（因果順守・§3.5.4）
    │ 2. 戦略 → SizingPort Decorator → エンジン（§3.5.5）
    │ 3. 結果ペイロード JSON を書き出し  4. 台帳を「完了」へ
    ▼
[照会]
  ブラウザ ── 1 秒間隔 ──> GET /sim/jobs/{job_id}  （NFR-04: 100ms 以内）
    │ 状態が「完了」になったらポーリング停止
    ▼
[閲覧]
  ブラウザ ──> GET /sim/data/{job_id}/report.json
    │ 多窓チャート／取引履歴テーブル／ヒートマップ／比較判定／接点／IS-OOS 切替／用語集
    ▼
  双方向連動（純状態機械 linkage を移植元から流用）
    マーカー hover → 取引 id → 該当行ハイライト ＋ 区間外ローソク減光（α=0.15）
    行 hover      → 取引 id → 該当マーカー強調
```

### 4.4 業務フロー・ユースケース

- 主アクター：戦略開発者（1 人が「チャート観察」「検証投入」「結果判断」を連続して行う）。
- 業務フロー：
  1. ライブまたはリプレイのモードでチャートを観察し、仮説（指標構成・水準）を得る。
  2. シミュレーションモードへ切り替える（同一ページ・再ナビゲーションなし）。
  3. 戦略項目（entry / SL / TP・将来はトレーリング / 部分決済）と資金管理項目（サイジング規則）を指定する。
  4. ジョブを投入し、状態表示が完了になるのを待つ。
  5. 結果を閲覧し（多窓・双方向連動・ヒートマップ・比較判定）、戦略の採否を判断する。
- ユースケースの位置付け：既存 UC-001（バックテストを 1 run 実行する）と UC-004（結果レポートを生成する）を、
  UI から起動・閲覧する経路として束ねるものである。既存 UC の出力形式は変更しない。

### 4.5 既存モジュールへの改変範囲（全量）

NFR-06 に基づき、既存改変は以下に限定する。これ以外の既存行は変更しない。

| 対象 | 改変内容 | 分類 |
|---|---|---|
| `unified_ui/web/js/mode_ui_view.js` | モード定義表の 3 値化・`applyModeUi` の表駆動化 | OCP 違反の是正 |
| `unified_ui/web/js/unified_root.js` | `toggle` の既定算出を表駆動化 | OCP 違反の是正 |
| `unified_ui/router.py` | 振り分け表へ 1 行追加・`sim_upstream` 引数追加・CLI 引数追加 | 表への追加（既定値ありで後方互換） |
| `unified_ui/web/js/sw_rewrite.js` | モード prefix の表駆動化 | OCP 違反の是正 |
| `unified_ui/web/sw.js` | 受理モード判定の表駆動化 | OCP 違反の是正 |
| `unified_ui/web/js/routed_fetch.js` | 許可モード集合の表駆動化（誤配の遮断） | 不具合の是正（§3.5.6 #9） |
| `indigators/indicator_ui/web/js/adapter/front/app_chrome_view.js` | ツールバーのボタン定義を配列で受ける形へ拡張 | OCP 違反の是正（既定は現状と同一） |
| `unified_ui/serve.sh` | sim コアの起動追加・`SIM_PORT` 追加 | 追加 |
| `tools/web_suites.txt` | 新規 web スイートの登録 | 台帳への追加（C-5） |
| `simulator/usecase/ports.py` | `SizingPort` の**追加**（既存 Port は変更しない） | ISP に沿った追加 |

無改変を保証する対象：`simulator/usecase/run_backtest.py`・`simulator/domain/account.py`・
`simulator/domain/order.py`・`simulator/adapter/strategy/**`（6 戦略）・`simulator/report_ui/**`・
`simulator/replay_ui/**`・`indigators/indicator_ui/**`（上記 1 ファイルを除く）。

---

## 5. データ設計

> 出力元：S-3 データ設計

### 5.1 データモデル概要

概念モデルは 2 系統に分かれる。系統 1 は「実行の管理」、系統 2 は「結果の表示」である。
系統 2 は移植元 `report_ui` の JSON 契約と同形とし、フロントの純ロジックを流用可能にする（NFR-05）。

```
系統 1（実行の管理）
  SimulationJob (1)
   ├─ 識別子・投入時刻・状態（受付 / 実行中 / 完了 / 失敗 / 取消）・終了時刻・失敗理由
   ├─ StrategySpec       戦略項目の指定（entry / SL / TP・将来 トレーリング / 部分決済）
   ├─ SizingSpec         資金管理項目の指定（口座条件とサイジング規則）
   └─ DataSpec           対象シンボル・時間足・期間（IS / OOS の分割点を含む）

系統 2（結果の表示・移植元と同形）
  ReportPayload (1)
   ├─ Meta
   ├─ Segment[is] / Segment[oos]  （各: Meta / Report / Bars / Trades / Orders / Agg）
   ├─ Summary{is,oos}
   ├─ Degradation{metric → {is,oos,ratio,delta}}
   └─ Verdict{result, reasons[]}
```

### 5.2 主要エンティティ定義

| エンティティ | 概要 | 主要属性 | 関連エンティティ |
|---|---|---|---|
| SimulationJob | 1 回のバックテスト実行の管理単位 | 識別子, 状態, 投入時刻, 終了時刻, 失敗理由 | StrategySpec, SizingSpec, DataSpec, ReportPayload |
| StrategySpec | 戦略項目の指定 | 使用指標と設定値, entry 条件, SL 条件, TP 条件, 建玉変更条件（TBD-01 依存） | SimulationJob |
| SizingSpec | 資金管理項目の指定 | 初期残高, 証拠金率, ロスカット維持率, 単位あたり価値, サイジング規則と設定値 | SimulationJob |
| DataSpec | 対象データの指定 | シンボル, 時間足, 開始, 終了, IS/OOS 分割点 | SimulationJob |
| ReportPayload | 表示用の結果全体 | meta, segments, summary, degradation, verdict | SimulationJob, Segment |
| Segment | 1 区間（IS または OOS）の完全データ | label, meta, report, bars, trades, orders, agg | Trade, Bar, Agg |
| Trade | 確定トレード 1 件 | id, side, entry/exit の時刻・価格, profit, volume, sl, tp, hold_sec, balance | Segment |
| Agg | 区間集計（グラフ・ヒートマップ・接点の源） | entries 系, pl 系, balance_curve, heat, contacts | Segment |

> 系統 2 のキー構成は移植元 `report_ui` の JSON 契約と同一とする。契約の詳細キー一覧は
> `.doc/sim-report-ui/基本設計書.md` §5.2 を参照し、本書では再定義しない（単一ソース・C-8）。
> `Agg.contacts`（接点）は移植元 `chart.js:88-102` が消費する系列であり、本機能でも同名・同形とする。

### 5.3 データフロー図

- 生成：DataSpec（対象データ）→ 指標供給（因果順守）→ 戦略 → SizingPort Decorator → 実行エンジン →
  結果 → ReportPayload → JSON（ジョブごとの新規パス）。
- 消費：JSON → フロント → 多窓チャート・テーブル・ヒートマップ・比較判定。
- 既存データ（marketdata・fixtures・confirmation）は読み取り専用であり、本機能は書き込まない。

### 5.4 データライフサイクル

- ジョブ台帳と結果ペイロードは、ジョブごとの新規パスに生成する。既存データを上書きしない。
- 結果ペイロードは同一の指定から再生成可能であるため、バックアップの対象としない。
- 保持期間・削除方針は未確定（§9.3 TBD-10）。無制限に蓄積すると容量を消費するため、
  上限件数または保持日数のいずれかを定める必要がある。

---

## 6. インターフェース設計

> 出力元：S-3 インターフェース設計

### 6.1 API 設計概要

- API 種別：REST 相当（HTTP・JSON）。既存 core と同じく `http.server` 上に実装する。
- ブラウザから見えるパスは全て `/sim/` 配下であり、ルータが prefix を除去して sim コアへ渡す
  （既存 2 モードと同一方式・`router.py:52-55` の表に従う）。

| パス（ブラウザから） | メソッド | 概要 |
|---|---|---|
| `/sim/` および `/sim/js/*`・`/sim/vendor/*` | GET | sim モードの静的資産（shared_js_root フォールバック経由の共有 JS を含む） |
| `/sim/jobs` | POST | ジョブ投入。StrategySpec / SizingSpec / DataSpec を受け、ジョブ識別子を返す |
| `/sim/jobs/{job_id}` | GET | ジョブ状態の照会（受付 / 実行中 / 完了 / 失敗 / 取消・失敗理由） |
| `/sim/jobs/{job_id}/cancel` | POST | ジョブの取消（FR-12） |
| `/sim/jobs` | GET | ジョブ一覧（投入履歴の閲覧） |
| `/sim/data/{job_id}/report.json` | GET | 完了ジョブの結果ペイロード |
| `/sim/indicators` | GET | 選択可能な指標の一覧（§3.5.4 の因果性検定を通過したもののみ） |

- バックエンド境界（新規・既存 Port は変更しない＝ISP）：

  | 境界 | 責務 | 配置層 |
  |---|---|---|
  | `SizingPort`（新規） | 口座状態と規則から発注量を決定する。式の権威は `account_engine` | usecase（`simulator/usecase/ports.py` へ追加） |
  | ジョブ台帳 Port（新規） | ジョブの登録・状態更新・照会を抽象化する | sim コア usecase |
  | ジョブ起動 Port（新規） | 計算の子プロセス起動を抽象化する（テストで差替可能にする） | sim コア usecase |
  | 指標供給 Port（既存 `IndicatorPort` を実装） | ライブ compute 由来の指標系列を戦略へ供給する | simulator adapter |

  > `StrategyPort`・`IndicatorPort`・`TickModelPort`（`simulator/usecase/ports.py:108-161`）は変更しない。

### 6.2 画面構成・遷移

| 画面／領域 | 構成 | 対応機能 |
|---|---|---|
| ツールバー（既存） | モード切替（ライブ / リプレイ / シミュレーション） | FR-01, FR-02 |
| 実行指示パネル | 戦略項目の指定・資金管理項目の指定・対象期間の指定・投入操作 | FR-03, FR-05, FR-06, FR-10 |
| ジョブ状態表示 | 状態・経過・失敗理由・取消操作 | FR-11, FR-12 |
| チャート領域 | 価格 / Balance / Drawdown の 3 窓縦積み・論理レンジ相互同期 | FR-15 |
| 下部タブ：取引履歴 | 取引履歴テーブル・双方向連動・区間外減光 | FR-13, FR-14 |
| 下部タブ：ヒートマップ | 曜日 × 時間帯 | FR-16 |
| 下部タブ：比較・判定 | IS / OOS 劣化比較・判定バナー | FR-17 |
| 下部タブ：用語集 | 指標の定義 | FR-20 |
| チャート上トグル | 接点マーカーの表示 / 非表示 | FR-18 |
| 区間セレクタ | IS / OOS の切替 | FR-19 |

- 画面遷移：単一ページ。モード切替・タブ切替・区間切替はページ遷移を伴わない
  （既存 2 モードの方式を踏襲・`.doc/LIVE_REPLAY_UNIFICATION_BASIC_DESIGN.md` §3）。

### 6.3 外部システム連携仕様

| 連携先 | 連携方式 | データ形式 | 頻度 | エラー時動作 |
|---|---|---|---|---|
| ライブ core（8001） | 本機能からは連携しない（誤配の遮断が要件・§3.5.6 #9） | — | — | — |
| indicator_ui の compute 実装 | Python の読み取り専用 import 境界（`_indicator_ui_bridge` と同方式） | Python オブジェクト | ジョブ実行時 | import 失敗はジョブを「失敗」へ遷移させる |
| marketdata（価格データ） | 既存の読み取り経路 | 既存準拠 | ジョブ実行時 | 読込失敗はジョブを「失敗」へ遷移させる |
| バックテスト CLI（子プロセス） | プロセス起動・終了コード・標準出力 | JSON（結果ペイロード） | ジョブごと 1 回 | 異常終了は「失敗」へ遷移。部分的な結果を公開しない |
| ブラウザ ↔ ルータ | HTTP GET / POST | JSON / HTML / JS | 操作ごと・状態照会は 1 秒間隔 | sim コア停止時はルータが 502 を返し、ライブ・リプレイは無影響（NFR-02） |

### 6.4 通信プロトコル・データ形式

- プロトコル：HTTP（公開 8000 のみ外部公開。内部ポートは loopback 限定）。
  選定理由：既存 2 モードと同一機構を用いるため（C-3）。
- データ形式：JSON。選定理由：移植元 `report_ui` の契約と同形にすることが NFR-05 の前提であり、
  形式を変えると純ロジックの流用ができなくなる。Protobuf / MessagePack は新規依存であり C-3 に反する。
- 状態通知の方式：ポーリング（1 秒間隔）を採用する。

  | 案 | 概要 | 既存機構との整合 | 採否 |
  |---|---|---|---|
  | 案 P：ポーリング（採用） | クライアントが 1 秒間隔で状態を取得 | 既存 core は全て GET / POST のみで構成され、Service Worker のリライト機構も同期要求を前提とする | **採用** |
  | 案 Q：Server-Sent Events / WebSocket | サーバから状態を送出 | Service Worker のリライト対象外となり、ルータのプロキシ実装（`router.py` は `do_GET` / `do_POST` のみ実装・実測 :305-309）も対応していない | 棄却 |

  - 採用根拠（依頼者裁定 5＋実測）：`router.py:305-309` が `do_GET` / `do_POST` のみを実装しており、
    長時間接続の中継機構を持たない。案 Q はルータの大幅改変を要し NFR-06 に反する。

### 6.5 ポート割当

| 用途 | ポート | 状態 |
|---|---|---|
| 公開（ルータ） | 8000 | 既存・固定 |
| ライブ core | 8001 | 既存・固定 |
| リプレイ core（統合配下） | 8281 | 既存・固定 |
| リプレイ core（単独起動） | 8280 | 既存（`simulator/replay_ui/serve.sh:31`） |
| report_ui 単独ビューア | 8770 | 既存（`simulator/report_ui/serve.py:12`） |
| **sim コア（統合配下）** | **未確定** | §9.3 TBD-03。既存採番と衝突しない値を依頼者裁定で確定する |

---

## 7. 非機能設計

> 出力元：S-4 品質特性の担保

### 7.1 性能設計・スケーラビリティ対策

| 要件 | 数値目標 | 設計対策 | 測定方法 |
|---|---|---|---|
| NFR-01 負荷分離 | ライブ core の request path 占有 0 秒 | sim コアを別プロセス化し、さらに計算を子プロセスへ出す（§3.2） | ジョブ実行中に `/live/candles` の応答時間を実測し、非実行時との差が計測誤差内であることを確認 |
| NFR-03 投入応答 | 1 秒以内 | 投入 API は受理と台帳登録と子プロセス起動のみを行い、計算完了を待たない | 実 HTTP で投入から応答までを実測 |
| NFR-04 状態取得 | 100ms 以内・ポーリング間隔 1 秒 | 状態照会は台帳の読み出しのみ。結果ペイロードを読み込まない | 実 HTTP で連続 100 回の応答時間分布を実測 |
| 指標供給の計算量 | 1 万バーで 1 秒以内 | 全期間 1 回計算（案 ii・§3.5.4）を既定とする。逐次計算（案 i・実測 6.6ms/回＝1 万バーで約 66 秒）は照合基準としてのみ使う | 両案の所要時間と系列一致を実測 |
| 描画性能 | 移植元と同等（数千件規模の取引で操作可能） | 移植元の可視レンジ絞り込みと上限（`chart.js:20` の上限 700・`:310-314` の可視レンジ絞り）をそのまま流用 | ブラウザで hover 応答を実測 |

### 7.2 可用性設計・障害対策

| 要件 | 数値目標 | 設計対策 | 測定方法 |
|---|---|---|---|
| NFR-02 障害分離 | sim コア停止時、ライブ・リプレイの成功率 100% | ルータ配下のプロセス分離。sim のみ 502 | sim コアを停止させ、`/live/*` と `/replay/*` の応答を実測 |
| ジョブ異常時の整合 | 部分的な結果ペイロードの公開 0 件 | 子プロセスの正常終了時のみ結果を公開状態にする（fail-stop） | 異常終了を起こす入力でジョブを投入し、結果が公開されないことを確認 |
| ジョブの取り残し | 孤児プロセス 0 件 | sim コア停止時に実行中ジョブの子プロセスを終了させる。停止は明示的な操作で行う（detached 起動をしない） | 停止後にプロセス一覧を実測 |

> 本機能はローカル開発用途であり、稼働率（99.9% 等）の対象外である。該当なしを明示する。

### 7.3 セキュリティ設計

- 認証・認可：ローカル開発用途のため認証を設けない。内部ポート（ライブ・リプレイ・sim コア）は
  loopback 限定とし外部公開しない（既存方針を踏襲・`router.py:366-373` の既定値が `127.0.0.1`）。
- 通信暗号化：localhost のため TLS を用いない（該当なし）。
- 入力検証：ジョブ投入の入力（戦略項目・資金管理項目・期間）は sim コアの受付 UC で検証する。
  検証を通らない入力は受理せず、ジョブを作らない。
- パストラバーサル対策：静的配信は既存 `static_file_server` の dual-root ガードを用いる。
  同実装は `Path.is_relative_to` による区切り境界一致で CWE-22 を封じる（`static_file_server.py:80-88` に
  実装意図と根拠が記載されている）。sim コアはこの実装を再利用し、独自の解決処理を書かない。
- 子プロセス起動：起動引数にユーザー入力を文字列連結で埋め込まない（シェル経由の起動をしない）。
  入力は台帳のファイルとして渡し、子プロセスがそれを読む。
- 監査ログ：ジョブの投入・状態遷移・失敗理由を記録する（§7.4）。

### 7.4 運用・保守性設計

- ログ設計：sim コアはジョブ投入・状態遷移・子プロセスの終了コードを記録する。
  子プロセスの標準出力・標準エラーはジョブごとのファイルへ保存し、失敗理由の追跡に用いる。
- 監視ポイント：ジョブの状態分布（実行中の滞留件数）・子プロセスの終了コード・結果ペイロードのサイズ。
- 構成管理：sim コアは `simulator/sim_ui/` 配下に集約する（`replay_ui` / `report_ui` と同じ粒度）。
  フロントの新規スイートは `tools/web_suites.txt` へ登録する（C-5・未登録のスイートは
  `tools/tests/test_web_suites_ledger.py` が検出する仕組みが既に存在する）。
- 保守性（責務分離）：実行の管理（系統 1）と結果の表示（系統 2）を JSON 契約で分離する。
  指標の追加は指標供給の登録追加で完結し、モードの追加はモード定義表への追加で完結する（OCP）。
- バックアップ・リストア：結果ペイロードは再生成可能であるためバックアップ対象としない。

---

## 8. 開発・運用方針

> 出力元：S-4 品質特性の担保

### 8.1 開発方法論・プロセス（段階分割）

Phase 1 から実測検証可能な最小単位で刻む。各 Phase の通過条件を満たさない限り次へ進まない。

| Phase | 内容 | 通過条件（すべて満たすこと） | TBD 依存 |
|---|---|---|---|
| 1 | モード機構の 3 値化＋sim コアの骨格＋ルータ `/sim/*` | 1. 既存の JS スイート（`tools/web_suites.txt` の 4 件）が全て緑。2. 3 モードを切り替えても各モードの API が正しい core へ届く（`routed_fetch` の誤配が無い）ことを**実 HTTP で実測**。3. sim コアを停止させたとき `/sim/*` のみ 502 で、ライブ・リプレイが無影響であることを実測。4. **ブラウザ実 UI** で 3 モードの切替を確認 | なし（TBD-03 のポート採番のみ先行して必要） |
| 2 | ジョブ実行系＋SizingPort（Decorator） | 1. ジョブ投入から完了までが実 HTTP で成立。2. 投入応答 1 秒以内・状態取得 100ms 以内を実測。3. ジョブ実行中に `/live/candles` の応答時間が非実行時と差が無いことを実測（NFR-01 の直接検証）。4. SizingPort 経由の発注量が `account_engine` の権威式と一致することを検定。5. **既存戦略・エンジンが 1 行も変わっていないことを差分で確認**。6. 既存 Python テストが全て緑（MT5 突合の回帰なし）。7. Decorator が戦略ごとの明示指定でのみ適用され、既存サイジングを持つ戦略へ一律適用されないことを検定 | TBD-13・TBD-14（Phase 2 完了時） |
| 3 | 指標供給＋因果性検定 | 1. 全期間 1 回計算（案 ii）と逐次計算（案 i）の系列が全バーで一致する指標を列挙。2. 一致しない指標は選択不可として明示されることを確認。3. 1 万バーの供給が 1 秒以内であることを実測 | TBD-05 |
| 4 | 表示移植（骨格） | 1. v5.2.0 で 3 窓・論理レンジ同期・取引履歴テーブルが描画される。2. **マーカー hover で区間外ローソクが α=0.15 で減光する**ことをブラウザ実 UI で実測。3. 双方向連動（マーカー ⇄ 行）が両方向で動作することを実測。4. パリティ点（骨格分）が移植元と一致 | TBD-04（接点の別系列） |
| 5 | 表示移植（周辺） | 1. ヒートマップ・比較判定・接点マーカー・IS/OOS 切替・用語集が描画される。2. **パリティ点の一致率 100%**（NFR-05）。3. 接点マーカーの独立トグルが売買マーカーに影響しないことを実測 | TBD-04 |
| 6 | 戦略項目の指定（entry / SL / TP） | 1. UI で指定した条件がジョブ結果に反映されることを実測。2. 同一指定の再実行で結果が一致（決定性） | なし |
| 7 | 建玉変更（トレーリング・部分決済） | **TBD-01 の裁定が下るまで着手しない。** 裁定後に通過条件を確定する | **TBD-01・TBD-02** |

- 各 Phase は「git → architecture → tdd → programmer → code-review → git」の 6 フェーズで進める
  （プロジェクト規約・`.doc/LIVE_REPLAY_UNIFICATION_BASIC_DESIGN.md` §6 と同一）。

### 8.2 品質保証方針

- 各 Phase で既存テストの全通過を回帰確認する（NFR-06 の保証）。
- 対象は Python テスト（`simulator/**/tests`）と JS スイート（`tools/web_suites.txt` の登録分）の両方。
- MT5 突合の回帰は「`run_backtest.py` と 6 戦略の差分が 0 行であること」で構造的に保証する。
  差分が発生する設計（§3.5.3 案 B / 案 C）を採る場合は、突合の golden 一致を回帰条件へ追加する。

### 8.3 テスト方針

- 単体：モード定義表の走査（3 値化）／ジョブ状態遷移の規則／SizingPort の決定式（`account_engine` との一致）／
  移植した純ロジック（`balanceForwardFill` 等）／モード prefix のリライト規則。
- 結合：ジョブ投入 → 子プロセス → 結果ペイロードの形状検証／ルータの `/sim/*` 振り分けと 502 隔離／
  指標供給の因果性検定（案 i と案 ii の系列一致）。
- システム：ブラウザ実 UI での 3 モード切替・投入・状態表示・結果閲覧・双方向連動・減光。
- 受入：パリティ検定（移植元 `report_ui` とのパリティ点一致・NFR-05）。方式は
  `.doc/REPORT_UI_PARITY_DESIGN.md` のパリティ点方式を踏襲する。
- **検証経路の制約（NFR-09）**：計算関数の直接呼び出しや合成データを検証根拠にしない。
  実 UI・実 HTTP 経路で確認する。

### 8.4 リリース・デプロイメント方針

- 環境構成：ローカル開発のみ（開発＝本番）。外部デプロイの対象外。
- 起動：`unified_ui/serve.sh` が router（8000）＋ライブ core（8001）＋リプレイ core（8281）＋
  sim コア（TBD-03）を起動・監視する。生 python 起動と臨時ポートを使わない（NFR-08）。
- 停止：明示的な操作で停止する。detached 起動をしない。
- 実験用の直接起動を行った場合は、作業終了時に serve.sh 構成へ戻す。

---

## 9. リスク・課題

> 出力元：S-1 リスク列挙 ／ S-5 設計検証

### 9.1 技術的リスクと対策

| リスク | 影響度 | 発生確率 | 対策 | 対策の出典／根拠 |
|---|---|---|---|---|
| R-1 `StrategyPort` 境界と `run_backtest.py` 無改変が両立せず、FR-07 / FR-08 が設計不能のまま実装へ進む | 高 | 高（実証済みの構造的衝突） | Phase 7 を TBD-01 の裁定まで着手しない。Phase 1〜6 は裁定に依存しない範囲で完結させる | 実証（§3.5.3 の実証 1〜5） |
| R-2 `routed_fetch.js:69` の未知モード live フォールバックにより、sim の API がライブ core へ誤配され、エラーにならず誤結果を返す | 高 | 高（現行コードの挙動） | Phase 1 で許可モード集合の表駆動化を実施し、実 HTTP で誤配 0 件を実測する | 実測（`routed_fetch.js:67-70` を Read） |
| R-3 v4 の系列生成 API が v5.2.0 に存在せず、移植元 `chart.js` の vendor 依存部が全面書き換えとなる | 中 | 確定（実測済み） | Phase 4 で API 置換を実施。純ロジックと純状態機械は無改変で流用し、書き換え範囲を vendor 呼出に限定する | 実測（v5.2.0 バンドルに `addCandlestickSeries` 等が 0 件） |
| R-4 v5 の `createSeriesMarkers` が同一系列への複数ハンドル付与に対応せず、売買マーカーと接点マーカーの独立トグルが成立しない | 中 | 未検証 | Phase 4 / 5 の通過条件で実測する。不成立の場合は移植元と同じく別系列へ分離する（移植元 `chart.js:13-15` が既に別系列方式） | 実測（移植元の別系列化理由は v4 のシリーズ単位上書き） |
| R-5 全期間 1 回計算（案 ii）と逐次計算（案 i）の指標系列が一致せず、因果順守が崩れる指標がある | 高 | 中 | Phase 3 で指標ごとに検定し、一致した指標のみを選択可能にする。不一致の指標は無音で使わせない | 実測（`causal_compute.py:1-11` の truncate による因果保証） |
| R-6 ジョブの同時実行数が無制限となり、ホストの資源を食い潰す | 中 | 中 | 同時実行数の上限を定める（TBD-06）。上限は「並列度の制限」ではなく「受理の可否」として設計し、待ち行列で滞留させない | プロジェクト規約（上限は仕事の量に置く） |
| R-7 MT5 突合系の口座規約と OANDA 規約の取り違えにより、発注量が誤った証拠金体系で決定される | 高 | 中 | SizingPort の実装は `account_engine` の関数のみを呼ぶ。`domain/account.py` を参照しない。境界の依存を検定で固定する | 実測（2 体系の相違・§3.5.3 実証 6） |
| R-8 sim コアの内部ポートが既存採番と衝突し、起動が失敗するか他サービスへ接続する | 中 | 中 | TBD-03 で採番を確定してから Phase 1 に着手する。起動時に占有チェックを行う | 実測（8000 / 8001 / 8280 / 8281 / 8770 が使用中） |
| R-9 新規 web スイートが `tools/web_suites.txt` に未登録となり、テストの欠落が気付かれない | 中 | 中 | Phase 1 で台帳へ登録する。台帳と実在スイートの一致は既存の検定が強制する | 実測（`tools/web_suites.txt:1-21` の目的記載と ISSUE-280 の経緯） |
| R-10 ISSUE 番号 362 が 2 件重複しており（ISSUE.md:6698 と :6723）、根拠の参照先が一意に定まらない | 低 | 確定（実測済み） | 本書では「ISSUE.md の行番号」を併記して一意化する。番号重複そのものの是正は本機能の範囲外 | 実測（同一番号の別 Issue が 2 件存在） |
| R-11 サイジング規則の権威が二重になる（`weekly_vol_band` は資金比リスクのサイジングをドメインに既に持つ） | 高 | 確定（実測済み） | Decorator の適用を戦略ごとの明示指定とし、一律適用しない。対象戦略の扱いを TBD-13 で確定してから Phase 2 を完了させる | 実測（`domain/volatility_band.py:6, 50`・`weekly_vol_band.py:68, 71`） |
| R-12 発注量が決定時点の推定建値に基づき、エンジンが決める実際の建値と乖離する | 中 | 確定（実測済み） | 推定であることを設計上明示し、乖離の許容範囲を TBD-14 で定める。ティック経路は bid / ask が引数で渡るため乖離しない | 実測（`run_backtest.py:300-304` の `derive_quotes`／`on_new_bar` の引数に価格が無いこと） |

### 9.2 スケジュール・リソースリスク

- Phase 1〜6 は TBD-01 の裁定に依存しない。裁定が遅れても Phase 6 まで到達できる。
- Phase 7（FR-07 / FR-08）は TBD-01 の裁定なしに着手できない。裁定内容によって実装量が
  「新 UC の新設（大）」から「既存 UC への拡張点追加（中）」まで変わるため、
  裁定前に工数を確定できない。
- Phase 4 / 5（表示移植）は移植元の資産が実在するため、仕様の不確実性は小さい。
  ただし v4 → v5 の API 置換は実測どおり全面的であり、Phase 4 の作業量は小さくない。

### 9.3 今後の検討課題（TBD 一覧・すべて依頼者確認が必要）

| 項目 | 確認が必要な理由 | 確認先／確認方法 |
|---|---|---|
| **TBD-01（最重要）実行エンジンの境界** | 依頼者裁定 2 の「別ユースケースとして並立させる構成にはしない」に 2 通りの解釈が成立し、うち解釈 2 は同一文中の「無改変で据え置き」と両立しない。`StrategyPort` の唯一の消費者が `run_backtest.py` であることは実証済みであり、FR-07 / FR-08 は同ファイルの改変なしに実現できない。設計側で一方的に決定できない | 依頼者裁定。§3.5.3 の案 A / 案 B / 案 C から選択を得る |
| **TBD-02 SizingPort が参照する口座体系** | Decorator が評価する口座状態を、エンジンが持つ MT5 体系の口座で見るか、OANDA 体系（`account_engine`）で別途評価するかが TBD-01 に連動する。両体系は維持率の定義が異なる（実証 6） | 依頼者裁定（TBD-01 と同時） |
| **TBD-03 sim コアの内部ポート** | ポートは固定運用であり新規採番は依頼者の承認事項。既存は 8000 / 8001 / 8280 / 8281 / 8770 が使用中 | 依頼者承認。Phase 1 着手前に必要 |
| TBD-04 v5 の同一系列への複数マーカーハンドル | 売買マーカーと接点マーカーの独立トグルの実現方式が決まらない。移植元は v4 の制約により別系列に分離している | Phase 4 の通過条件として同梱 vendor（v5.2.0）で実測 |
| TBD-05 指標ごとの因果性 | 全期間 1 回計算で因果順守が成立する指標が特定されていない。推測で断定しない | Phase 3 で案 i と案 ii の系列一致を指標ごとに実測 |
| TBD-06 ジョブの同時実行数の上限 | 上限なしでは資源を食い潰す。上限値は運用判断 | 依頼者確認。既定案は 1（直列）とし、実測後に見直す |
| TBD-07 発注量の丸め規則 | 算出量が最小取引単位・刻みを満たさない場合の扱い（切下げ / 切上げ / 拒否）が未確定。`Order.validate` は刻み違反を `InvalidPriceError` とする（`domain/order.py:41-42` に刻み比の許容が実在） | 依頼者確認 |
| TBD-08 ジョブ取消の意味 | 取消時に子プロセスを終了させるか、完了まで走らせて結果を破棄するかが未確定 | 依頼者確認 |
| TBD-09 NFR-03 / NFR-04 の数値 | 投入 1 秒・状態取得 100ms は実務的推奨（仮説）であり実測に基づかない | Phase 2 で実測し、必要なら数値を改める |
| TBD-10 結果ペイロードの保持方針 | 保持期間または上限件数が未定。無制限の蓄積は容量を消費する | 依頼者確認 |
| TBD-11 戦略項目の指定形式 | 「インジケーター駆動の entry / SL / TP」をどの表現力（比較演算・論理結合・履歴参照）まで指定可能にするかが未確定。表現力を広げるほど UI と検証の複雑度が増す | 依頼者確認。Phase 6 着手前に必要 |
| TBD-12 sim モードの単独起動の要否 | リプレイは統合配下（8281）と単独（8280）の両方を持つ。sim も単独起動を要するかが未確定 | 依頼者確認（TBD-03 と同時） |
| **TBD-13 既存サイジングを持つ戦略の扱い** | `weekly_vol_band` は `N = f_risk · Capital / (O − S)` を既にドメインで実装している（実測）。sim モードの対象に含めるか、含める場合に SizingPort とドメインのどちらを権威とするかが未確定。設計側で決めるとサイジング規則の権威が二重になる | 依頼者裁定。Phase 2 完了前に必要 |
| **TBD-14 決定時点の建値と実際の建値の乖離許容** | 実際の建値はエンジン内部の `derive_quotes` が決め、発注量の決定時点には渡らない（実測）。バー経路の発注量は推定建値に基づく。乖離をどこまで許容するか、あるいはティック経路のみを sim モードの対象とするかが未確定 | 依頼者確認。Phase 2 着手前に必要 |

---

## 10. 付録

### 10.1 用語集

| 用語 | 定義 |
|---|---|
| モード | 統合 UI が持つ表示・操作の系統。ライブ / リプレイ / シミュレーションの 3 値 |
| シミュレーションモード | 本機能で追加する第 3 モード。バックテストの実行指示と結果閲覧を担う |
| sim コア | シミュレーションモードのバックエンド。ルータ配下の別プロセス |
| ジョブ | 1 回のバックテスト実行の管理単位。受付 / 実行中 / 完了 / 失敗 / 取消 の状態を持つ |
| 戦略項目 | インジケーター駆動の entry / SL / TP / 建玉変更の規則。`StrategyPort` が境界 |
| 資金管理項目 | 口座状態（残高・維持率）に依存する発注量の決定規則。`SizingPort` が境界 |
| 因果順守 | バー i の指標値がバー i 以前のデータのみから決まること |
| パリティ点 | 移植元と移植先の一致を判定する観測点。`.doc/REPORT_UI_PARITY_DESIGN.md` の方式 |
| 権威式 | 計算規則の唯一の定義元。口座規約では `account_engine` の関数群 |
| 表駆動 | 分岐を定義表の走査へ置換し、要素追加時に本体を変更しない構造 |
| IS / OOS | In-Sample（学習区間）／ Out-Of-Sample（検証区間） |
| 接点マーカー | 指標と価格の交差点を示すマーカー。売買マーカーとは別概念・別配色 |

### 10.2 設計判断の根拠・トレードオフ

| 判断項目 | 採用 | 代替 | 根拠 | 出典区分 |
|---|---|---|---|---|
| プロセス構成 | ルータ配下の別 core＋計算の子プロセス化 | ライブ core / リプレイ core への相乗り | ISSUE-362（ISSUE.md:6698）・ISSUE-364（同 :6782）の実測に整合。502 隔離の前例あり | 実証＋規約 |
| sim コア内部構造 | `replay_ui` 同型のクリーンアーキ | 薄い HTTP サーバ | 既存前例の実在。状態遷移をドメインへ置く（SRP） | 公式（Martin 2017）＋規約 |
| SizingPort の結線 | `StrategyPort` の Decorator（戦略ごとの明示適用） | 各戦略への直接注入／エンジン内での差替／全戦略への一律適用 | 戦略・エンジン双方を無改変にできる唯一の案（実証 2〜5）。一律適用は既存サイジングを持つ戦略で権威が二重になるため採らない（実証 1b） | 公式（Decorator / OCP）＋実証 |
| 発注量が用いる建値 | 決定時点の推定建値（バー経路）／bid・ask（ティック経路） | 実際の建値を用いる | 実際の建値はエンジン内部の `derive_quotes` が決め、決定時点には渡らない（実証 6・7）。乖離の許容は TBD-14 | 実証＋TBD |
| 口座規約の権威 | `account_engine` の関数群のみ | 各所での式の再実装 | ISSUE-369 で参照実装が確定。式の二重定義を作らない | 実証（ISSUE.md:6951）＋規約 |
| 指標供給の既定 | 全期間 1 回計算（因果性検定を通過した指標に限る） | バーごとの逐次計算 | 逐次は実測 6.6ms/回で 1 万バー約 66 秒。ただし正しさの基準は逐次に置く | 実証＋実務的推奨（仮説） |
| 多窓構成 | 3 チャートインスタンス維持・API のみ置換 | v5 の pane 機能へ統合 | パリティ検定の合格条件を変えないため | 規約（NFR-05） |
| マーカー hover 検出 | `param.hoveredObjectId`（v5.2.0 で実測済み） | 座標近接の自前ヒットテスト | 前者はブラウザ実測で 2 度確認済み。後者は実測で破綻が確定している（dy ≈ −130） | 実証（ISSUE.md:387・CHART_TRADE_MARKERS 詳細設計 §13.1） |
| 状態通知 | ポーリング（1 秒間隔） | SSE / WebSocket | ルータが `do_GET` / `do_POST` のみ実装（実測 :305-309）。長時間接続の中継機構を持たない | 実証＋規約 |
| 3 値化の方式 | モード定義表の単一ソース化と全分岐の表駆動化 | 分岐に `sim` を追記 | 第 4 モード追加時に本体を変更しない状態を到達点とする。既存に表駆動の前例あり（`sw_rewrite.js:41-43`） | 公式（OCP）＋実証 |
| 結果契約の形式 | 移植元と同形の JSON | 独自形式 | 純ロジック・純状態機械の流用が NFR-05 の前提 | 規約 |
| FR-07 / FR-08 の実現方式 | **未確定（TBD-01）** | — | 上流裁定に 2 通りの解釈が成立し、実証で解決できない | TBD |

### 10.3 参考資料

- 既存設計書：
  - `/workspaces/app/.doc/LIVE_REPLAY_UNIFICATION_BASIC_DESIGN.md`（ルータ方式・502 隔離・無波及の前例）
  - `/workspaces/app/.doc/sim-report-ui/基本設計書.md`（表示側の JSON 契約・パリティ・移植元の設計）
  - `/workspaces/app/.doc/sim-report-ui/詳細設計書.md`（`linkage.js` の位置付け・マーカー id 規約）
  - `/workspaces/app/.doc/CHART_TRADE_MARKERS_DETAILED_DESIGN.md` §13（hover 検出方式の実測記録）
  - `/workspaces/app/.doc/REPORT_UI_PARITY_DESIGN.md`（パリティ点方式）
  - `/workspaces/app/.doc/backtest/BACKTEST_CLEAN_ARCH.md`（層と Port の配置規約）
- 既存コード（本書の実証根拠）：
  - `simulator/usecase/ports.py:108-161`（`StrategyPort` / `IndicatorPort` / `TickModelPort`）
  - `simulator/usecase/run_backtest.py:89-106, 286-290, 305, 326-331, 818-824`
  - `simulator/usecase/account_engine.py:85-105, 310-335`
  - `simulator/domain/order.py:45-63`／`simulator/domain/account.py:40-92`
  - `simulator/adapter/strategy/*.py`（6 戦略の発注量）
  - `simulator/replay_ui/usecase/causal_compute.py:1-11, 30-36`
  - `simulator/replay_ui/adapter/_indicator_ui_bridge.py:1-15`
  - `simulator/replay_ui/framework/static_file_server.py:75-103`
  - `simulator/replay_ui/main/composition_root.py:30`
  - `simulator/report_ui/web/js/chart.js:3-5, 13-20, 42-102, 295-343`
  - `simulator/report_ui/web/js/linkage.js`（全 42 行）／`simulator/report_ui/web/js/main.js:143-162`
  - `unified_ui/router.py:52-55, 305-309, 316-324, 364-373`
  - `unified_ui/web/js/mode_ui_view.js:16, 68-78`／`unified_ui/web/js/unified_root.js:111-126`
  - `unified_ui/web/js/sw_rewrite.js:41-43, 63-81`／`unified_ui/web/sw.js:25-34`
  - `unified_ui/web/js/routed_fetch.js:67-70`／`unified_ui/serve.sh:56-70`
  - `indigators/indicator_ui/web/js/adapter/front/trade_markers_renderer.js:91-108, 339-344`
  - `indigators/indicator_ui/web/js/adapter/front/chart_renderer.js:4, 23-24, 128, 644`
  - `indigators/indicator_ui/web/js/adapter/front/app_chrome_view.js:48, 79-82`
  - `tools/web_suites.txt`
- ISSUE 記録：ISSUE-025（ISSUE.md:380-392・v5.2.0 hover 実測）／ISSUE-362（同 :6698・GIL 律速）／
  ISSUE-364（同 :6782・単一ワーカー詰まり）／ISSUE-369（同 :6951・口座状態エンジン）／
  ISSUE-370（同 :6921-6949・計算機統合の保留と着手条件）

### 10.4 SOLID 原則への写像

| 原則 | 本設計での適用 |
|---|---|
| SRP | sim コアの層分離（実行の管理 / 結果の表示を JSON 契約で分離）。ジョブ状態遷移をドメインへ置き HTTP ハンドラへ埋めない |
| OCP | モード定義表による 3 値化（第 4 モード追加時に本体不変）。`StrategyPort` Decorator による発注量差替（戦略・エンジン無改変）。指標の追加は登録追加で完結 |
| LSP | Decorator は `StrategyPort` の契約（`on_new_bar` / `on_tick` が発注列を返す・`on_position_check` が hold / close を返す）を保つ。差し替えても呼出側の前提を壊さない |
| ISP | `SizingPort` を新設し既存 Port を変更しない。指標供給は `IndicatorPort` の面のみに依存する（`_indicator_ui_bridge` の粒度別アクセサと同方針・ISSUE-136 の前例） |
| DIP | sim コアの usecase はジョブ台帳・ジョブ起動を抽象へ依存させ、実装（ファイルシステム・子プロセス）を外側へ追い出す。テストで差替可能にする |

### 10.5 上流入力の検証結果（`upstream-input-validation`）

| # | 上流入力（依頼者裁定） | 抽出した前提 | 実証手段 | 判定 |
|---|---|---|---|---|
| U-1 | 裁定 1：unified_ui へ第 3 モードを追加する | 統合 UI がルータ方式で 2 モードを収容している | `unified_ui/router.py:52-55` を Read | **採用** |
| U-2 | 裁定 2：口座規約は `account_engine` に一本化・MT5 突合系は無改変 | `account_engine` が権威式を持つ／MT5 突合系が別体系である | `account_engine.py:310-335`・`domain/account.py:40-92` を Read | **条件付き採用**（前提は実証。ただし文言に 2 解釈が成立＝TBD-01） |
| U-3 | 裁定 3：`StrategyPort` / `IndicatorPort` を境界とする | 両 Port が実在し、境界として機能している | `usecase/ports.py:108-144` を Read | **採用** |
| U-4 | 裁定 3：トレーリング・部分決済はエンジン未実装（grep 0 件） | 実装が存在しない | `run_backtest.py` を grep／`domain/order.py:60-63`／`trade_markers.py:77` を Read | **採用（根拠を強化）**。単に未実装ではなく、`Order.validate` が `trailing_stop` を明示的に拒否する |
| U-5 | 裁定 3：`StrategyPort` を境界に上記を追加設計する | `StrategyPort` の消費者を変更せずに追加できる | `simulator/`（tests 除く）を grep：消費者は `run_backtest.py` のみ | **棄却**（C-6 と両立しない。§9.3 TBD-01 へ） |
| U-6 | 裁定 3：戦略無改変で量の決定を Port へ委譲 | 発注量の差替点が戦略の外に存在する | `run_backtest.py:89-106, 286-290, 305, 818-824`・`domain/order.py:45-52` を Read | **採用**（Decorator 方式で成立・§3.5.5） |
| U-7 | 裁定 3：全 6 戦略が `volume=cfg["lot_size"]` 固定 | 6 戦略すべてが発注量を固定値で確定している | `adapter/strategy/` を grep／`weekly_vol_band.py:57-72`・`domain/volatility_band.py:6, 40-50` を Read | **条件付き採用**。固定値は 5 戦略のみ。`weekly_vol_band` は `N = f_risk · Capital / (O − S)` の資金比リスクサイジングをドメインに既に持つ。「全 6 戦略が固定」は成立しない（R-11・TBD-13） |
| U-8 | 裁定 4：simulator 内蔵指標は 2 本のみ | `adapter/indicator/` の構成 | Glob：`ema_adx_di.py` / `madiff.py` / `registry.py` / `__init__.py` | **採用** |
| U-9 | 裁定 4：`causal_compute` と `_indicator_ui_bridge` が先行例 | 両者が実在し因果順守・読み取り専用再利用を担う | `causal_compute.py:1-11`・`_indicator_ui_bridge.py:1-15` を Read | **採用** |
| U-10 | 裁定 5：ライブ request path へ載せない（ISSUE-362 / 364） | 両 ISSUE が実測記録として存在する | `ISSUE.md:6698, 6782` を Read | **採用**（ISSUE-362 は番号重複あり＝R-10） |
| U-11 | 裁定 6：3 値化で触る箇所は 9 点（全量） | 9 箇所が実在し、それ以外に 2 値分岐がない | 9 箇所すべてを Read で照合 | **採用（訂正あり）**。`unified_root.js:111` の `toggle` は既に明示ターゲット引数を受ける／`sw_rewrite.js:41-43` に表駆動の前例が既にある |
| U-12 | 裁定 7：`report_ui` の完全再現・`linkage.js` と純ロジックは流用可 | `linkage.js` が DOM / vendor 非依存である | `linkage.js` 全 42 行を Read | **採用** |
| U-13 | 裁定 7：vendor 層のみ v4.1.3 → v5.2.0 へ移植 | 移植範囲が vendor 層に限定される | 両 vendor の版を Read／v5.2.0 バンドルを grep | **採用（作業量の訂正）**。v5.2.0 に v4 の系列生成 API が 0 件のため、vendor 依存部は全面置換となる |
| U-14 | 裁定 7：v5.2.0 のマーカー hover 検出は未検証 | 検出可否が未確認である | `ISSUE.md:387`（ブラウザ実測・RESOLVED）／`CHART_TRADE_MARKERS_DETAILED_DESIGN.md:364-366`／`trade_markers_renderer.js:96-103` を Read | **棄却**（実測により反証済み。§3.5.2） |
| U-15 | 裁定 7：NG 時の代替は座標→マーカー索引の自前ヒットテスト | 当該方式が実現可能である | `CHART_TRADE_MARKERS_DETAILED_DESIGN.md:367-371` を Read（実測 dy ≈ −130・原理的に成立しない） | **棄却**（破綻が実測済みの方式を選択肢に並べない） |

- **残存リスク（本設計の範囲外）**：TBD-01 / TBD-02 の裁定内容によっては §3.5.3 の案 B / 案 C が選ばれ、
  その場合は MT5 突合の golden 回帰条件を §8.2 へ追加する必要がある。本書はこの追加条件を定義していない。

### 10.6 関連する標準・規格

- Robert C. Martin『Clean Architecture』（2017）— 依存方向・境界・SOLID の原典。
- Gamma ほか『Design Patterns』（1994）— Decorator パターン（§3.5.5 の採用方式）。
- RFC 7230 §6.1（hop-by-hop ヘッダ）— 既存ルータのプロキシ実装が準拠（`router.py:57-72`）。
- CWE-22（パストラバーサル）— 既存 `static_file_server` の dual-root ガードが対処（`:80-88`）。
- プロジェクト絶対遵守ルール（`.claude/CLAUDE.md`）— 破壊的変更禁止・応急処置禁止・実証なき断定の禁止。

---

## 設計検証（S-5 自己点検）

- **要求整合**：FR-01〜FR-20 を §4.1 の機能一覧へ全マッピングした。要件未対応の設計要素・
  設計未対応の要件はない。ただし FR-07 / FR-08 は実現方式が TBD-01 に依存する状態であり、
  「設計済み」ではなく「裁定待ち」として明示した。
- **技術的実現性**：Phase 1〜6 の実現性は実測で確認した。ルータの表駆動化・core の同型新設・
  Decorator による発注量差替・純ロジックの流用は、いずれも既存の実在物を根拠とする。
  Phase 7 の実現性は TBD-01 の裁定に依存し、本書では確定していない。
- **非機能対応**：NFR-01〜NFR-09 に設計対策と測定方法を記載した。NFR-03 / NFR-04 の数値は
  実務的推奨（仮説）であり、Phase 2 の実測で確定する（TBD-09）。
- **セキュリティ網羅**：ローカル用途のため認証・暗号化は該当なしを明示した。パストラバーサルは
  既存ガードの再利用、子プロセス起動はシェル非経由と入力のファイル受け渡しで対処する。
- **保守性・拡張性**：モード追加は定義表への追加、指標追加は登録追加、サイジング規則の追加は
  `SizingPort` の実装追加で完結する。いずれも本体コードを変更しない（OCP）。
- **残課題**：§9.3 に TBD-01〜TBD-14 を集約した。実証で解決できない前提（文言の意図・
  指標の因果性・v5 のマーカーハンドル仕様・性能数値・サイジング権威の所在・建値の乖離許容）は
  所与とせず、裁定または実測へ委ねた。
- **上流入力の検証**：15 件を実証し、採用 11 件・条件付き採用 2 件・棄却 2 件と判定した（§10.5）。
  棄却した 2 件（v5.2.0 の hover 未検証・座標近接の代替案）は実測により反証されたものであり、
  未検証事項として残すと実証済みの事実を未確定として扱う誤りになるため、本書では訂正して採用した。

---

## 11. Phase 1 着手前の裁定記録（2026-08-11・依頼者承認済み）

設計内容確認（architecture-executor による実コード全数照合）で検出された裁定事項に対し、
依頼者が以下を承認した。本節は §3.5.6・§4.5・§6.5・§9.3 の該当記述に優先する。

### 11.1 裁定内容

| # | 対象 | 裁定 |
|---|---|---|
| 1 | **TBD-03（sim コア内部ポート）** | **統合配下 8381 のみ採番**（全リポジトリ grep で空きを実証済み）。8380 は単独起動用に予約。 |
| 2 | **TBD-12（単独起動の要否）** | **保留**。Phase 1 では `simulator/sim_ui/serve.sh`（単独起動）を作らない。 |
| 3 | **L-1（ツールバー構成の注入経路）** | **承認**。`indigators/indicator_ui/web/js/adapter/front/composition_root_front.js` の `bootstrap` へツールバー構成の注入オプションを追加する（既定値は現行と byte 等価・後方互換。既存検定 `color_theme_toolbar_mount.test.js` で固定）。NFR-06 の改変範囲を本件に限り拡張する。 |
| 4 | **L-2（モード別 CSS の 3 値化）** | **承認**。`unified_ui/web/index.html` のモード別 CSS を 3 値化する。`#replay-bar` の表示条件は「replay モードのときだけ表示」へ反転させ、表駆動と整合させる。 |
| 5 | **D-2（sim フロントの所在）** | **`simulator/sim_ui/web`**（replay_ui と同型・core が自分の web を所有し `/sim` 経由で配信・共有 JS は shared_js_root フォールバック）。§3.1 の「unified_ui/web 配下」の記述は誤りとして本裁定で上書きする。 |
| 6 | **V-8（router の拡張方式）** | **upstreams マッピング化を承認**。`create_router_server(bind_addr, *, upstreams: Mapping[str, str], web_root, ...)` と繰り返し指定 CLI `--upstream <mode>=<url>` へ変更する（§3.5.6 #5/#6 の「per-mode 引数追加」方式を置換）。既存 `unified_ui/tests/test_router.py` の呼び出し 4 箇所は同時更新し、既存挙動はテストで固定して移行する。 |

### 11.2 改変対象の全量確定（D-1 の解消）

改変対象は「§3.5.6 の 11 箇所（8 ファイル）＋設計内容確認で検出された 4 箇所」の**計 15 箇所**とする。
NFR-06 の「9 箇所」表記・§3.5.6 注記の「9 点と同一集合」表記は本節の 15 箇所へ読み替える。
追加 4 箇所：

| ID | 箇所 | 内容 |
|---|---|---|
| L-1 | `composition_root_front.js:130-158, 273` | `bootstrap` へツールバー構成注入オプション（裁定 3） |
| L-2 | `unified_ui/web/index.html:14-24` | モード別 CSS の 3 値化（裁定 4） |
| L-3 | `mode_ui_view.js:82-95` | `wireModeSwitchButtons` を「ボタン id→目標モード」の表から `toggle(target)` 明示指定へ |
| L-4 | `op_log.js:108` | mode 判定をモード定義表の走査へ |

### 11.3 D-3（serve.sh の改変範囲）の確定

`unified_ui/serve.sh` の改変は :60-62（`SIM_PORT=8381` 追加）に加え、sim core の起動・監視・停止・
router 引数の各行（:57・:65・:167・:217・:244-245・:250-252・:255-256・:262-263・:267-270 相当）に及ぶ。

### 11.4 Phase 1 で作らないもの（YAGNI・設計内容確認の判定を採用）

- `simulator/sim_ui/{domain,usecase,adapter}/` の空パッケージ（Phase 2 の F-3 で、ジョブ状態遷移という実在の変更要因とともに作る）
- ジョブ台帳 Port・ジョブ起動 Port の先行定義
- `simulator/sim_ui/web` の JS テストスイート枠（テスト実体を書く時点で `tools/web_suites.txt` へ同一コミットで登録。空枠登録は台帳検定が赤になる）
- 静的配信の sim 専用ラッパ（`simulator.replay_ui.framework.static_file_server` を import で再利用・複製禁止）

---

## 12. Phase 2 着手前の裁定記録（2026-08-11・依頼者承認済み）

本節は §3.5.5・§9.3 の該当記述に優先する。

### 12.1 TBD-13 の裁定：サイジングは設定オプション（戦略リストのハードコード禁止）

- 6 戦略は **MT5 突合検定のために移植したテスト実装**であり、製品戦略ではない（依頼者言明）。
  サイジング仕様はテスト戦略に依存しない汎用オプションとして設計する。
- **サイジング（ケリー・破産確率基準）は全戦略共通の設定オプション（既定 OFF）**。
  - **OFF（既定）**: 既存挙動と byte 等価。MT5 突合の回帰なし。
  - **ON**: `SizingPort`（Decorator）が発注量の**唯一の権威**として戦略の出す量を一律上書きする。
    `weekly_vol_band` の自前サイジングも特別扱いせず上書き対象（「ON なら SizingPort が唯一の権威」
    という規則で二重権威を構造的に排除）。
- 戦略ごとの明示指定リスト（§3.5.5 の「戦略ごとの明示指定」）は**ハードコードとして実装しない**。
  適用可否は設定（config）のみで決まる。

### 12.2 TBD-14 の裁定：推定建値の乖離はロット刻み吸収基準

- バー経路の成行注文でのみ、発注量計算に使う推定建値と実際の建値（`derive_quotes`）に
  スプレッド分の差が生じる（実取引の成行注文と同一の構図・シミュレータ特有の欠陥ではない）。
- **合格基準**: この差に起因する発注量の差が `volume_step`（ロット刻み）未満に収まることを
  Phase 2 の検定で実測固定する。刻みを跨ぐケースは**保守側（少ない方）へ丸める**。
- 指値・逆指値（約定価格＝指定価格）とティック経路（bid/ask が引数で渡る）では差は生じない。

### 12.3 Phase 2 の絶対制約（依頼者厳命）

1. **SOLID 絶対遵守**: 全設計判断を 5 原則へ写像して提示する。
2. **既存ファイル無改変**: 実装は新規ファイルの追加のみ。Phase 1 で作成したファイルも「既存」として
   扱う。完了時に既存ファイルの `git diff` が 0 行であることを通過条件へ追加する。
   追加だけで届かない箇所＝拡張点の欠如（OCP 違反）は、勝手に改変せず、該当箇所と最小の
   拡張点設計を提示して依頼者承認を得る。
3. **複製絶対禁止**: 再利用は import のみ。発注量・証拠金・ロスカットの式の権威は
   `account_engine` の関数群のみとし、式・既存処理の手書き複製を作らない。

### 12.4 拡張点欠如の承認（E-1・E-2・2026-08-11 依頼者承認済み）

§12.3-2 の「既存ファイル無改変」に対する承認済み例外は次の 2 件のみ（これ以外の既存コード改変は禁止）:

| ID | 箇所 | 承認された最小改変 |
|---|---|---|
| E-1 | `unified_ui/serve.sh:304-311` | sim core 起動のインライン import 2 行をジョブ対応版 composition root（`build_sim_job_app`＋対応 serve）へ差し替え（約 3 行） |
| E-2 | `simulator/main/__init__.py` | `build_interactor` へ任意引数 `strategy_decorator`（既定 None）を追加し、None なら既存と byte 等価（+2 行）。byte 等価は golden 検定で固定 |

記録文書（本設計書・ISSUE.md 末尾追記）を除き、差分に現れてよい既存ファイルはこの 2 つだけである
（機械的ゲート: `git diff --name-status 95afbd5` で検査）。

### 12.5 E-3 の裁定: 価格系列を供給できない戦略は投入時拒否

成行の建値推定に使える価格系列を指標レジストリが持たない戦略（例: MA_Slope_EA＝ema のみ）への
sizing ON 指定は、ジョブ受付時に明示エラーで拒否する（無音の誤動作を作らない・新規ファイルのみで実現）。

### 12.6 E-4 の裁定: 「エッジと破産確率 — f を決める」の Python 完全実装

- `integrated_position_sizing_calculator.html` は**プロトタイプ（Python 移植用）**である（依頼者言明）。
- 移植範囲は **Step 1「エッジと破産確率 — f を決める」のみ**（入力 p・R・破産水準・α・T・N →
  出力 EV・フルケリー f*・RoR(f)・破産確率制約 f（RoR ≤ α の最大 f）・参考等額厳密解 (q/p)^N）。
  Step 2/3（f 選択・分割ロット変換）は移植対象外。
- 実装は参照実装（HTML の該当 JS）を読み、そのアルゴリズムに厳密準拠する。着手前の参照実装読解は必須。
- 決定性: バックテストの「同一入力→同一出力」要件のため、モンテカルロの乱数はシードを設定項目化
  （既定は固定値）。プロトタイプのシード無し `Math.random` はこの点のみ Python 仕様として確定させる。
- 位置づけ: プロトタイプの本実装化であり、§12.3-3 複製禁止の「複製」には当たらない。実装後は
  Python 側が権威となる。

### 12.7 E-5 の裁定: ジョブは並列実行（2026-08-11 依頼者裁定）

- **複数のバックテストジョブをバックグラウンドで並列実行できる仕様とする**（依頼者裁定。
  「同時 1 本」案は棄却）。ジョブは投入受付ごとに独立の**子プロセス**として即時起動する。
  プロセス並列のため GIL 律速（ISSUE-362）を受けず、「重い計算をサーバの request path に
  載せない」原則（ISSUE-362/364）も維持される。
- サイジングが参照する口座体系は OANDA（`account_engine`）— §11 裁定（口座規約一本化）を適用。
- 取消（FR-12）: 子プロセスへ SIGTERM・状態「取消」で終端確定・部分結果は非公開（fail-stop）。

### 12.8 E-6 の承認と SL 無し注文の扱い（2026-08-11 依頼者承認済み）

- **E-6 承認**: E-2 の機械的帰結として、`simulator/tools/walk_forward_cli.py` の引数ミラー
  `_BUILD_INTERACTOR_KEYWORDS` へ `"strategy_decorator",` を 1 行追加する。§12.4 の許可リストへ
  本ファイル（当該 1 行のみ）を追加し、許可 M は計 5 ファイルとなる。
  ミラー自体が手書き複製（単一ソース違反）である恒久是正は ISSUE-372 系の申し送りへ追記する。
- **SL は sizing ON の必須前提条件（依頼者裁定 2026-08-11）**: ケリー基準は損切り幅が無ければ
  1 トレードのリスク額を定義できず、f→ロット変換が成立しない。したがって:
  1. **受付時検証（主）**: sizing ON のジョブは、戦略設定が SL を保証すること（SL 系パラメータの
     存在・正値）を投入時の必須項目として検証し、満たさなければ E-3 と同じ受付検証で明示拒否する。
  2. **実行中 fail-stop（従・防御の二重化）**: 受付検証をすり抜けて SL 無し注文が出た場合は
     内部不変条件違反としてジョブを明示失敗（failure_reason 記録）。無音スキップ（取引ゼロ）と
     素通し（元ロット維持＝権威の二重化）はいずれも禁止。

### 12.9 公開面の bind 制限（🔴-5・2026-08-11 依頼者承認済み）

- Phase 2 で認証なしの変更系エンドポイント（`POST /sim/jobs`＝子プロセス起動）が新設されたため、
  公開ポート 8000 の bind を全 IF から **127.0.0.1 へ限定**する。`unified_ui/serve.sh` が router へ
  `--host 127.0.0.1` を渡す（1 行）。E-1 で許可済みの `unified_ui/serve.sh` の改変範囲を本行に限り拡張する。
- 併せて受付側の防御: ジョブ投入の `backtest` キーは `inspect.signature(build_interactor)` 由来の
  許可集合へ限定（未知キー明示拒否・手書き表禁止）、`sims`/`horizon` へ上限（1 ジョブの仕事量の上限
  であり並列度の制限ではない）。

### 12.10 変更系エンドポイントの要求元検査（🔴-A・2026-08-11 承認済み）

- bind 限定（§12.9）は他ホストからの到達を閉じるが、同一ホストのブラウザからの CSRF 型 POST
  （`Content-Type: text/plain` の simple request）は loopback 経由で到達しうる。これを閉じるため、
  変更系（`POST /sim/jobs`・`POST /sim/jobs/{id}/cancel`）は要求元を検査し cross-site を 403 で拒否する。
- 判定順: `Sec-Fetch-Site: same-origin` 許可 → `Origin` 不在（非ブラウザ）許可 → `Origin`/`Host` 比較。
  検査点は sim core（`serve_sim_jobs.py`・新規ファイル）に置く。`unified_ui/router.py` の `_HOP_BY_HOP` は
  `host` を含むが `origin`/`sec-fetch-site` を含まないため、両ヘッダは上流へ素通しされ検査が機能する。
- **次 Phase への設計制約**: sim フロントは公開ルータと同一オリジンで配信すること（別オリジン配信だと
  全投入が 403 になる）。`Sec-Fetch-Site` 非対応の旧ブラウザは `Origin`/`Host` 比較へ落ちるため、
  正規経路でも 403 になりうる（fail-closed の意図的選択・対応: Chrome 76+/Firefox 90+/Safari 16.4+）。

---

## 13. Phase 3 実装時の裁定記録（2026-08-11・実測起因の契約改訂）

Phase 3「指標供給＋因果性検定」（F-5 / FR-09）の TDD 工程で、§3.5.4 の前提 2 件が実測により
崩れたため、Phase 3 新規ファイル内に閉じる契約改訂を裁定した（既存ファイルは無改変のまま）。

### 13.1 崩れた前提（実測・jp225_tick / 5m・50,000 バー）

- 「load_source は 1 回」（§3.5.4 案 i の計算量見積りの前提）は `causal_compute` の入口形状上
  成立しない。呼出ごとに load_source（DataFrame→dict 実体化・実測 242ms）が走り、案 i は
  実測 0.25 秒/バー（見積り 6.6ms の約 38 倍）。
- 系列単位の Port 契約（series_upto → SeriesPoint|None）では、1 回の compute が全系列を生成する
  実装（`full_compute`）に対し検定コストが系列数ぶん多重化する（母集合 26 組・計 137 系列）。

### 13.2 裁定（すべて Phase 3 新規ファイル内・既存無改変）

1. **系列束契約**: `CausalSeriesProbePort` を bundle 契約へ改訂
   （`series_full/series_upto` が全系列を名前付きで返す）。系列名衝突は明示エラー。
   時系列でない kind（horizontal_line）は対象外とし台帳へ記録。
2. **load_source メモ化**: `MemoizedCausalComputePort`（全メソッド委譲 Decorator・
   `(ref, timeframe, csv_mtime)` 鍵・load_source のみメモ化）。式（compute）・因果規約
   （truncate）は `causal_compute` 内のままで FR-09 / C-7 非抵触。寿命は検定 CLI と
   ジョブ子プロセスに限定し常駐 sim core へは注入しない。実測効果: 200 バーで
   7.23 秒 → 0.60 秒（-91.7%）。
3. **cost-gating（段 0/1/2）**: 段 0 = 供給窓 10,000 本で供給コスト 1 回計測（1 秒超は
   `supply_cost_exceeded` とし案 i を走らせない）→ 段 1 = データセット先頭から N 本
   （--verify-bars 既定 1000）で全バー突合 → 段 2 = 段 1 一致分を供給窓で確定
   （coverage=1.0）。検定窓を「先頭から N 本」に取るのは、limit=None の下で案 i の各窓が
   案 ii の窓の prefix になる唯一の取り方（案 i の定義を変えない）。
4. **選択可否は系列単位・reason は 3 値固定**
   （`mismatch` / `supply_cost_exceeded` / `verification_incomplete`・自由文は detail）。
   台帳 conditions に supply_bars / verify_bars / verify_coverage / timeout を記録し
   `/sim/indicators` 応答にも返す（被覆の限界を明示）。
5. **fail-closed の強化**: 突合到達値の NaN は「比較できない＝不一致側」へ倒す
   （レビュー 🔴-1 の是正）。台帳の要素型不正は 503 へ翻訳。

### 13.3 通過条件の実測結果（2026-08-11）

- 通過条件 1（一致指標の列挙）: 供給窓 10,000 本の全バーで max_abs_diff=0.0 の 3 系列を確定
  （moving_averages:MA・profit_hlband:separate の hl_range・profit_osi_ma:osi_ma_kairi）。
  台帳は検定 CLI（`verify_indicator_causality_cli`）が機械生成。
- 通過条件 2（不一致の選択不可明示）: 全 137 系列が selectable + reason 3 値で台帳・API 応答に
  残ることをテストで固定（合成根経由の実 HTTP で 200/503 を実証）。
  公開 8000 経由の確認は TBD-P3-1（serve.sh 1 行・承認待ち・ISSUE-377）。
- 通過条件 3（1 万バー供給 1 秒以内）: 20 組が 0.042〜0.875 秒で通過。超過 6 組は
  `supply_cost_exceeded` として理由つき記録（最遅 profit_band:robust 4.281 秒）。
- 検定全体の実所要: 段 0 = 17.8 秒 / 段 1 = 242 秒 / 段 2 = 3309.5 秒（--verify-timeout 300）。
- 残課題は ISSUE-376（ULP 再現性・段 2 未確定 19 系列・warmup 扱い等）・ISSUE-377 に記録。

---

## 14. Phase 4 実装時の裁定記録（2026-08-11〜12・依頼者承認済み改変と実測）

Phase 4「表示移植（骨格）」（F-6 / FR-13〜15）の実装で、追加のみでは到達不能な経路が実測で
確定したため、依頼者承認のもと既存ファイル改変 9 件＋.gitignore 1 行を実施した。

### 14.1 承認済み既存改変（A〜H）

| ID | ファイル | 内容 |
|---|---|---|
| A | `unified_ui/web/js/unified_root.js` | sim 表示層の注入点（動的 import・simHandle の enable/disable。replay 注入と対称） |
| B | `unified_ui/serve.sh` | 合成根を `composition_root_display` へ 1 行差替 |
| C | `tools/web_suites.txt` | `simulator/sim_ui/web` を台帳へ 1 行追加 |
| D | `report_ui/web/js/chart.js` | 定数・純関数の export 付与＋構築規則 4 箇所の純関数抽出（挙動等価・回帰 e2e 30 本全緑） |
| F | `sim_ui/main/run_job.py` | run 成功後の report.json 書出し（失敗は job-dir へ記録・終了コード不変） |
| G | `report_ui/usecase/{build_report_payload,assessment_policy}.py` | 単一区間公開口 `execute_single`（segments={"single"}・degradation={}・verdict.result=""＝比較を騙らない）＋`not_evaluated`。既存 execute は byte 等価 golden で固定 |
| H-C | `unified_ui/web/index.html` | `body:not(.um-chart-api) .chart-wrap { display:none }` 1 行（版面所有者は統合層・反転記法） |
| H-D | `report_ui/tools/export_report_payload.py` | 43 行複製（int-time view 4 定義）を新規 `int_time_views.py` へ移設（as 束縛で参照不変） |

### 14.2 主要な構造裁定

- **移植元純ロジックの流用は symlink 不可**（StaticFileServer が resolve 後に許可根判定＝404 を実測）。
  prefix ルート（`/sim/report-js/`・`/sim/report-css/`・v4 vendor は恒久非配信）で複製 0 を実現。
- **sim 表示は iframe（同一オリジン `/sim/report_view.html`）**。style.css の :root/*/body 規則の
  統合ページ波及を文書境界で遮断（Shadow DOM は :root 解決不能の疑いで棄却）。
- **v5 レンダラの再入規律（R-A1〜A5）**: crosshair 購読 1 回・ハンドラ読み取り専用・vendor 書込を
  lock で囲み区間内の crosshair 由来通知を発行しない・linkage 一方向・減光は事前計算。
  原因は vendor 描画中の再入（hitTest 中の書込）で、v4 chart.js の lock イディオムと同型の是正。

### 14.3 通過条件の実測結果（2026-08-12・実ジョブ 236 trades）

1. v5.2.0 で 3 窓・論理レンジ同期・取引履歴テーブル描画: 成立（実ホイールで価格窓 {0,999}→{48,956} 同期・12 列完全一致）
2. マーカー hover 区間外減光 α=0.15: 成立（実グリフ画素 hover で dimmed=true・`.hl` 付与・call stack エラー 0 件）
3. 双方向連動: 成立（chart→table・table→chart の両方向を実測）
4. パリティ点（骨格 12 点）: 成立（verify_sim_display_parity.py 1 passed・ヘッダ規則 10 項目一致）
- TBD-04 実測: **成立**（同一系列へ createSeriesMarkers 2 ハンドル・独立トグル・hoveredObjectId 発火）
  → Phase 5 の接点マーカーは第 2 ハンドル方式を採用可（透明 LineSeries 不要）。
- 実測 C-1: live→sim→live の版面往復で live チャート寸法が完全復帰（H-C' 不要）。
- 申し送りは ISSUE-378。

---

## 15. Phase 5 実装時の裁定記録（2026-08-12・依頼者承認済み改変と実測）

Phase 5「表示移植（周辺）」（F-7 / FR-16〜20: ヒートマップ・比較判定・接点マーカー・IS/OOS 切替・
用語集）の実装で、追加のみでは到達不能な経路が実測で確定したため、依頼者承認のもと既存ファイル
改変 4 件（承認 R-1〜R-4・D-3）を実施した。

### 15.1 承認済み既存改変

| ID | ファイル | 内容 |
|---|---|---|
| R-1 | 技術スタック追加 | Chart.js v4.4.1（report_ui 同梱 chart.umd.js）を `/sim/report-vendor/chart.umd.js` の 1 ファイルだけ allowlist 配信（比較タブ 5 グラフ用）。v4 lwc は allowlist 非収録＝構造的不到達のまま |
| R-2 | `report_ui/web/js/compare.js` | `_renderVerdict` → `export renderVerdictBanner`（2 行・挙動等価） |
| R-3 | `simulator/main/__init__.py` | `build_ea_indicators`（EA が実行に使う指標系列の公開アクセサ）を追加のみで新設。build_interactor と同一フォールバック・既存経路無改変 |
| R-4 | `sim_ui/adapter/report_payload_writer.py` ほか | `_load_run_inputs` の既定束縛を `run_job.py`（main 層）へ移し adapter→main import を解消（DIP 是正・ISSUE-378 #7 同時是正） |
| D-3 | `report_ui/web/js/heatmap.js` | `buildHeatmap` へ opts 引数（既定＝現行 5 ビューと挙動等価。単一区間時に IS/OOS 損益差ビューを非表示） |

### 15.2 主要な構造裁定

- **ヒートマップは vendor 不要**（実測: Chart.js 不使用・HTML table＋linear-gradient の自前描画）。無改変 import で流用。
- **比較タブ 5 グラフのみ Chart.js 必須**（実測: `typeof Chart==="undefined"` で無音スキップ）。allowlist Decorator（`allowlist_file_routes.py`）で chart.umd.js 1 本だけ開け、v4 lwc の恒久遮断（NFR-07）と既存 404 検定を保つ。
- **接点マーカーは透明 LineSeries（value=close）＋第 2 createSeriesMarkers ハンドル**。v5 バンドルのマーカー価格解決の実測（value 系列は value がアンカー・OHLC 系列は high/low）により、candlestick への第 2 ハンドルではグリフ y 位置が移植元と変わるため LineSeries を採用（系列構造は移植元と同一・API のみ v5 流）。売買ハンドルと独立＝トグル独立（P5）。
- **実ジョブは常に単一区間**（segments={"single"}）。比較・区間切替は縮退表示（buildCompare を呼ばず verdict.reasons のみ掲示・canvas 0 件・segbtn 不在）。文言は payload が単一ソース。検定は 2 区間 fixture。
- **接点データ源**: R-3 アクセサ＋`contacts_supply`（`compute_segment_contacts` を import・写経 0）で EA の実系列から算出。非 MA EA は空（捏造しない fail-closed）。

### 15.3 通過条件の実測結果（2026-08-12）

1. ヒートマップ・比較判定・接点マーカー・IS/OOS 切替・用語集が描画される: 成立（実ジョブで接点 54 件・ヒートマップ 4 ビュー縮退・用語集 12 cards・比較タブは単一区間縮退表示）
2. パリティ点の一致率 100%（NFR-05）: 成立（周辺 16 点二画面突合 100% 一致＋Phase 4 の 12 点回帰 0）
3. 接点マーカーの独立トグルが売買マーカーに影響しない: 成立（接点 on/off で売買 badge 26→26 不変・on→off→on で画素一致・contactsToMarkers(visible:false)=[]）
- U-1〜U-5 実測: page errors 0（Chart.js と v5 lwc 同居）・.mv-pane はツールバー非被覆・実ジョブ規模で操作可・単一区間縮退成立。
- 申し送りは ISSUE-380。

---

## 16. Phase 6 実装時の裁定記録（2026-08-12・戦略項目の指定＋実行指示 UI）

Phase 6「戦略項目の指定（entry / SL / TP）」（F-8 / FR-05・FR-06）と、ブラウザ実 UI 完成のための
拡張（run config フォーム＋パネルのマウント）を実装した。

### 16.1 TBD-11 確定（依頼者裁定）
- entry = 比較演算＋AND 連鎖＋履歴参照 [bar-N]（レベル2）。OR・グループ化・>=/<=/==・インジケーター駆動 SL/TP は不可（参照実装 0 件）。
- SL/TP = 点数固定（既存 backtest.stop_loss_points/take_profit_points）。
- 完全被覆は TC24051901・ProFitBand の 2 戦略（実測）。ma_slope 系・StopEntryProbe・WeeklyVolBand は TBD-11 除外機構に依存し対象外。

### 16.2 承認済み既存改変
| ID | ファイル | 内容 |
|---|---|---|
| 注入方式 | simulator/main/__init__.py | build_interactor へ keyword-only 任意引数 strategy_override を追加のみ（既定 None で byte 等価）。決定は「専用 param 新設」（strategy_decorator フック濫用＝LSP 逸脱を避ける） |
| P6-E1〜5 | file_job_ledger.py / job_models.py / job_api_controller.py / run_job.py / submit_job.py | 第 3 の spec ブロック strategy の投入契約（既定 OFF で byte 等価）＋E-5 受付検証 |
| 拡張結線 | composition_root_display.py | ea-series・run-options の委譲 wrapper 結線（追加のみ・OCP・内側 byte 不変） |
| 拡張 dispatch | report_view.html | ?job= 有無で結果ビューア／実行指示フォームを出し分け |
| 回帰ゲート | import_source.test.js / _fakes.js | 複製 0 拡張＋front で非標準 `.children=`/`.parent` 使用禁止の構造ガード |
| 機械的帰結 | tools/walk_forward_cli.py | build_interactor シグネチャ同期（手書きミラー・test 強制・ISSUE-374-5 の対象） |

### 16.3 主要な構造裁定
- **汎用戦略は既存 StrategyPort を実装**（新規 Port 0）。条件評価は EntryConditions（domain・pandas/JSON 非依存）・SL/TP 点数算出は sltp_from_points に単一ソース化（6 戦略を写経しない・各自の複製は無改変で凍結）。ea_name は指標セット/ローダ/データ形式の選択にのみ効き、戦略は strategy_override で GenericConditionStrategy に置換。
- **銘柄仕様定数の単一ソース SymbolSpecCatalog を新設**（リポジトリに前例 0・CLI 4 箇所でハードコード不整合＝export_trade_markers は leverage=100 と他 10 の食い違いを実測）。JP225 の 8 定数は MT5 突合 fixture（case.yaml）由来で bit-exact ゲート（実保証は定数の直接等値 assert）で担保。data_path は marketdata registry（date/volume・バックテスト非対応）でなく MT5 形式実 JP225 CSV（time/spread）を指す。
- **run config フォームは profile を取得して 18 キー完全 body を組む**（data_path/symbol/period/8 定数/config_overrides は profile 由来・front リテラル 0）。既存 backtest verbatim 契約は widen しない（RunOptionsPort を投入経路に足さない）。
- **マウントは sim 子文書で ?job= dispatch**（不在→フォーム／有→ビューア）。投入成功後はユーザークリックで iframe 内遷移（ビュー自動介入禁止＝自動遷移しない）。統合ページ側改変 0。
- 実データは MT5 形式のみ存在するため、実 UI 完走のデモは MA_Slope_Pending_EA＋entry_price_basis=current_open（profile 供給）＋ema 条件（comma ローダ EA は実 JP225 データ 0 件・MT5 ローダ EA は close 系列を持たず open のみ＝実測）。

### 16.4 通過条件の実測結果（2026-08-12・実 UI フォーム・実 JP225 MT5 データ）
1. UI で指定した条件がジョブ結果に反映される: 成立（フォーム投入→job completed・bars=28097→ビューアに条件由来の建玉 trades=9681 反映。strategy 未指定は trades=10581＝override が実際に挙動を変える証左）。
2. 同一指定の再実行で結果が一致（決定性）: 成立（同一指定 2 回で stats.json byte 完全一致）。
- MT5 突合 bit-exact 不変（override None・reconcile 全緑）。full 2231 passed・sim_ui web 229 passed。
- 実ブラウザ専用バグ 2 件（`.children=`・`.parent`）を実 UI で検出・根治し構造ガード追加。
- 申し送りは ISSUE-381（本番データ配置・EA×データ適合制限・deposit 既定・パストラバーサル恒久是正・spec ブロック汎用化・gate (b) の negative control・walk_forward 同期）。
